# 04 · Risk Register

**Canonical source for: open + closed risks.** Living document — **update this when a risk
is opened, closed, or changes severity** (a PR that changes risk must touch this file).
Severity: P0 (critical) · P1 (high) · P2 (medium) · P3 (low). Status uses the
[taxonomy](./10_DOCS_INDEX.md#status-taxonomy). Only verified state is recorded here.

## Active risks
| ID | Sev | Status | Description / why it matters | Mitigation today | What closes it |
|----|-----|--------|------------------------------|------------------|----------------|
| RISK-001 | P1 | open · **staging + production applied & REST-verified PR #47/#48/#52/#55/#58** (staging + production migrations `0001`–`0014`, private bucket, 2 `authenticated` policies (0 unsafe), `0015` files grant, and **REST authz verification PASSED 14/14 in BOTH staging and production**; **only the [17 §5](./17_OMC_PRODUCTION_REPLACEMENT_PARITY_GATE.md) cutover checklist remains — RISK-001 stays OPEN**) | **Hosted apply is PARTIAL / INCOMPLETE** — *no longer "nothing applied to hosted Supabase."* Staging migrations `0001`–`0014` are **applied** to the **staging** project `ycdpzduxugdsffjqyoai`, the private `contract-files` Storage bucket **exists** (`public=false`, 25 MiB, `application/pdf`), and the `storage.objects` **object policies are now applied in staging** (2 `authenticated` INSERT/SELECT; **0 unsafe** — no UPDATE/DELETE/`FOR ALL`/anon/public; **structural verification passed**) — recorded in [25_STAGING_SCHEMA_AND_STORAGE_APPLY_EVIDENCE](./25_STAGING_SCHEMA_AND_STORAGE_APPLY_EVIDENCE.md) §0/§0.2. The **real Storage REST API authorization verification PASSED 14/14 in BOTH hosted staging (2026-06-18) and hosted production** (`dzbfxulvxchdemcettrx`, 2026-06-18 — [29](./29_PRODUCTION_STORAGE_APPLY_EVIDENCE.md); real Storage REST API calls with user-scoped JWTs, no service-role), and the production-discovered `public.files` `authenticated` SELECT/INSERT grant is codified as migration `0015`. Production synthetic cleanup is **complete** ([29 §6](./29_PRODUCTION_STORAGE_APPLY_EVIDENCE.md): synthetic Storage objects, Auth users, and business rows removed — counts 0; only 2 tenant rows + 3 append-only audit rows retained as anchors). **RISK-001 is materially reduced** by production apply + production verification + migration `0015` + this cleanup evidence — **but it remains OPEN** because **the [17 §5](./17_OMC_PRODUCTION_REPLACEMENT_PARITY_GATE.md) cutover checklist is still incomplete** (the only remaining closure criterion). Do not mark RISK-001 closed unless the risk register explicitly defines all criteria as satisfied. | Hosted-apply/Vercel/cutover discipline = runbook-of-record [20_STAGING_HOSTED_APPLY_AND_CUTOVER_DISCIPLINE](./20_STAGING_HOSTED_APPLY_AND_CUTOVER_DISCIPLINE.md) (staging-first; list→apply-expected-only→verify→smoke; stop/rollback; 10 checklists). Storage object-RLS **cannot be tested locally** (PR #41, [21](./21_STORAGE_LOCAL_HARNESS_FEASIBILITY.md)) → verified in hosted staging via the [22](./22_HOSTED_STORAGE_BUCKET_APPLY_RUNBOOK.md) runbook + the [23](./23_STORAGE_STAGING_APPLY_EVIDENCE_TEMPLATE.md) evidence template + the [24](./24_STAGING_ENVIRONMENT_VARIABLES_AND_WIRING_CHECKLIST.md) env-var checklist. PR #47/#48 recorded the staging schema + bucket apply, PR #52 the object-policy apply, PR #55 the **passed REST authz verification** ([25 §0/§0.2/§0.3](./25_STAGING_SCHEMA_AND_STORAGE_APPLY_EVIDENCE.md)). **Still partial — staging fully verified, but production not applied and cutover checklist unmet; production untouched.** | **Closure requires ALL of:** (1) ✅ reviewed **Storage object policies applied to staging** — **DONE** (PR #52, [25 §0.2](./25_STAGING_SCHEMA_AND_STORAGE_APPLY_EVIDENCE.md)): the [22 §5](./22_HOSTED_STORAGE_BUCKET_APPLY_RUNBOOK.md) policies (INSERT only when files metadata exists + contract-write authority, SELECT only when metadata readable, no UPDATE/DELETE/`FOR ALL`/public/cross-tenant; helpers via migration `0014`) are applied in staging and **structurally verified** (2 `authenticated` INSERT/SELECT, 0 unsafe); (2) ✅ the **[21 §6](./21_STORAGE_LOCAL_HARNESS_FEASIBILITY.md) real Storage REST API authorization verification COMPLETED and recorded** — **DONE** (PR #55, [25 §0.3](./25_STAGING_SCHEMA_AND_STORAGE_APPLY_EVIDENCE.md)): `scripts/verify-staging-storage-rest.mjs` run in hosted staging 2026-06-18, **14/14** (private bucket, no public/anon access, cross-tenant read+overwrite/delete denial, contract-write-only upload, no UPDATE/DELETE/`FOR ALL`, signed-URL single-object, files-row source of truth) + check-15 local `test-rls.sh` 222 — user-scoped JWTs, no service-role, production untouched; (3) ✅ the **production apply + production REST verification** — **DONE** (human-executed 2026-06-18, recorded [29](./29_PRODUCTION_STORAGE_APPLY_EVIDENCE.md)): migrations `0001`–`0014` applied to production, private bucket + 2 `authenticated` policies (0 unsafe), and `scripts/verify-production-storage-rest.mjs` passed **14/14** (user-scoped JWTs, no service-role); the discovered `public.files` grant is codified as migration `0015`; (4) ✅ **evidence recorded** ([29](./29_PRODUCTION_STORAGE_APPLY_EVIDENCE.md) + [25](./25_STAGING_SCHEMA_AND_STORAGE_APPLY_EVIDENCE.md)); (5) ⏳ the **[17 §5](./17_OMC_PRODUCTION_REPLACEMENT_PARITY_GATE.md) cutover checklist still satisfied** — **NOT yet met** (the hosted staging Auth + tenant-context verification PASSED 2026-06-18, 8/8 automated + manual Tenant A/B — [31 §7](./31_HOSTED_STAGING_AUTH_TENANT_CONTEXT_VERIFICATION.md), advancing §5 boxes 5/6/8; the synthetic-fixture cleanup is recorded ([31 §8](./31_HOSTED_STAGING_AUTH_TENANT_CONTEXT_VERIFICATION.md): tenant/org access removed; 2 profiles + 2 tenants + 2 Auth users retained as audit anchors because `audit_logs` is append-only); but the full RLS-suite-on-hosted re-run (prepared, **not yet run**, via the ref-guarded refusal gate `scripts/verify-staging-rls-suite.mjs` — [30 §6](./30_DOC17_CUTOVER_BLOCKER_SEQUENCE.md); raw `org_rls_test.sql` must never run against shared hosted staging — its `TRUNCATE`/`delete auth.users` fixtures are unsafe even rollback-only; run only against a disposable isolated project), the OMC-shaped dataset/critical-flow validation, and the remaining boxes are still open). **Remains OPEN — criteria (1)–(4) satisfied, but (5) the doc 17 §5 cutover checklist is not; apply+verification ≠ cutover.** |
| RISK-002 | P2 | open · **narrowed PR #20/#21/#23** (clarified PR #18; write-integrity closed PR #17) | **Child/link tables not (fully) org-scoped for reads.** Remaining cases (canonical map: [02 §8](./02_SECURITY_AND_RLS.md)): **tenant-only** — `people` + `files` (tenant members read; org-only users do not; `files` since `0013`); **default-deny** — `identity_accounts`, `license_rules`, `license_evaluations`, `invoices` (no read policy at all). No cross-tenant leak; org-only users read none of these. **Done for three tables (read-only):** `app_contracts` (`0006`), `app_users` (`0007`), and `app_user_identity_matches` (`0008`, read a match iff you can read the linked app_user — status only, no PII) are now **org-scoped for READ** — read-only, no DELETE. *Scope note:* tracks org-scoped **reads**; the write-integrity gap is closed by `0005` (RISK-C08). | `tenant_id` + RLS on all; `app_contracts` org-read (`0006`, **T28**) + `app_users` (`0007`, **T29**) + `app_user_identity_matches` (`0008`, **T30**); default-deny on 4 tables (`files` now tenant-member read — `0013`); same-tenant FKs (`0005`); map in [02 §8](./02_SECURITY_AND_RLS.md). | Org-scoped **read** policies + tests before surfacing `people` per-org or any default-deny table. **Not closed** — only `app_contracts` + `app_users` + `app_user_identity_matches` reads are done; `people` stays tenant-only, the rest default-deny. **No identity matching algorithm / people merge / UAR / orphaned status / provisioning** (design: [12_IDENTITY_MATCHING_READ_SCOPE](./12_IDENTITY_MATCHING_READ_SCOPE.md); `people`+`identity_accounts` intentionally NOT org-scoped). **`files` note (PR #33 design → #34 schema → #35 RLS → #40 validation):** the contract PDF/AI workflow is **designed** ([16_CONTRACT_PDF_AI_EXTRACTION_DESIGN](./16_CONTRACT_PDF_AI_EXTRACTION_DESIGN.md)); `0012` added the metadata columns + same-tenant `contract_id` FK + CHECKs; **`0013` (PR #35) added the tested `files` RLS policies** — SELECT `is_tenant_member` + INSERT contract-write authority (`can_write_contract`: tenant editor+ OR procurement-org manager; `paying_org_id` never grants write; `uploaded_by`=caller); **no UPDATE/DELETE/FOR ALL** (proven by **T34**); **PR #40 added pure server-side PDF validation + a server-derived tenant-bound storage-path helper** (`src/lib/files/pdf-validation.ts`, 16 unit tests) — a building block only. This **narrows** RISK-002 for `files` (read + write authority tested; validation ready) but **does NOT close it**: org-scoped `files` read is deferred, **`files` is still NOT surfaced** in the app (no DAL/route/UI; **no Storage bucket/upload action** — the bucket + object-RLS are hosted-gated, [20](./20_STAGING_HOSTED_APPLY_AND_CUTOVER_DISCIPLINE.md)), and `identity_accounts`/`license_*`/`invoices` stay default-deny. **RISK-002 remains OPEN.** |
| RISK-003 | P2 | open | **`resource_org_links` deferred.** Access is column-based (responsible/paying/procurement); richer multi-org sharing not modeled. | Column union read covers chargeback today ([02 §3](./02_SECURITY_AND_RLS.md)). | A relationship table + migration + tests when sharing outgrows columns. |
| RISK-004 | P3 | open | **Org hierarchy traversal deferred.** `organizations.parent_org_id` exists but parent-org roles don't inherit child visibility. | Flat org membership; acceptable for MVP. | Recursive membership + policy + tests. |
| RISK-006 | P2 | open (narrowed PR #13/#14) | **Minimal product UI.** Auth shell + read-only app inventory (`/apps`, PR #13) + read-only app detail (`/apps/[id]`, PR #14); still not usable for OMC (no app-user roster, contracts, invoices, files, license rules, reports, writes). | Foundation-first by design; read-only surfaces ship incrementally. | Build-sequence stages 5–9 + parity ([11](./11_LEGACY_PARITY_AND_OMC_CHECKLIST.md)). |
| RISK-012 | P2 | open (narrowed PR #9) | **No user provisioning, invites, or tenant switching.** Read-only tenant/org context resolution now lands (PR #9 — `resolveTenantContext`), but there is still no flow to create a tenant/membership and no UI to switch between multiple tenants (the resolver picks a deterministic first). A user with no membership reads as `no_membership` and sees nothing (correct under RLS, but product-incomplete). | RLS denies-by-default → completeness gap, not a leak; users provisioned out-of-band; active tenant chosen deterministically. | A provisioning/invite path + a tenant switcher when multi-tenant users exist, each with tests. |
| RISK-007 | P1 | open · **vault substantially IMPLEMENTED** (migrations `0017`-`0038`; ~79 files in `src/lib/server/connector-vault/`; §8-style connector RLS/grant tests `T38`-`T58` green in the CI RLS suite; `0029` runner-secret grants hosted-applied + verified in staging + production; design docs [19](./19_CONNECTOR_CREDENTIAL_VAULT_DESIGN.md)/[42](./42_CONNECTOR_CREDENTIAL_VAULT_DESIGN.md)) · **Partial mitigation (2026-07-03):** staging Slack client-secret live task-read → envelope-ingest path proven end-to-end in idcaddie-connector-runner PR #16. Source Slack SM secret scheduled for deletion and proven unreadable. **Further mitigation (2026-07-04, staging):** RUN GATE A ran — first real B2c Slack OAuth exchange, per-tenant token stored envelope-only in `connector_secrets`; the stored token was then decrypted (runner-only KMS capability) and used for a real Slack `auth.test` (200 ok), no plaintext logged (records criteria 6/7/8/10 in [docs/52](./52_RISK_007_CLOSURE_EVIDENCE_TRACKER.md); evidence in connector-runner `docs/STAGING_LIVE_RUN_EVIDENCE.md`). **RUN GATE B (2026-07-05/06, staging):** the vault-version rotation/supersede/revoke lifecycle ran - successor `oauth_access` v2 stored envelope-only, v1 revoked (reason `superseded`), post-revoke v1 fails closed while v2 stays usable (records criteria 12/13/14-events); **honest scope:** v2 wraps the **same Slack token** as v1 (Slack re-issued it), so a provider-side token rotation was NOT forced and provider-side `auth.revoke` was NOT run. Criterion 9 (replay/state reuse) is now hosted-proven on staging (2026-07-05/06): Gate 3A read-only (the RUN GATE A v2 oauth_pending state is consumed; the consume SQL blocks any re-consume) + Gate 3E a consume-only replay-check on ECS that failed closed as already_consumed - no Slack/OAuth code/KMS/token store/connector_secrets write/v3. **Still pending for closure:** permanent source-secret deletion after 2026-07-10 (15); the closure-decision judgment on whether a provider-side token rotation is required (12); plus the closure register (18) and Phase C unblock (19). Broader credential-vault closure remains pending. **Follow-up:** Confirm permanent deletion of `/idcaddie/staging/slack/oauth-client-secret` after the 2026-07-10 recovery window. **RISK-007 remains OPEN; Phase C remains BLOCKED.** Staging only (`ycdpzduxugdsffjqyoai`); production not touched. | **Credential vault: schema + server code substantially BUILT; broader hosted verification + the full connector path still pending.** Legacy stored 50+ secrets in plaintext; v3 does not port that. | Connector vault IS implemented (migrations `0017`-`0038`: `connector_secrets` Tier-2 DENY-ALL envelope store, `connector_app_secrets`, `connector_secret_lifecycle_events` append-only, `oauth_pending`, resolver/graph; ~79 files in `src/lib/server/connector-vault/` incl. envelope crypto + AWS KMS + server-only OAuth). The encrypted/service-role boundary is ENFORCED ([02](./02_SECURITY_AND_RLS.md), [01](./01_ARCHITECTURE.md)) — vault-ref-not-secret, redacted metadata, no service-role on request paths, related-org/payor get no credential authority, server-only OAuth, non-destructive sync — and is covered by §8-style connector RLS/grant tests `T38`-`T58` green in the CI RLS suite; `0029` runner-secret grants hosted-verified staging + production. The built vault follows the implemented design [42](./42_CONNECTOR_CREDENTIAL_VAULT_DESIGN.md) (two-tier split + envelope encryption), realizing [19](./19_CONNECTOR_CREDENTIAL_VAULT_DESIGN.md)'s invariants. **ONE** real STAGING app-level Slack OAuth client secret has been ingested envelope-only via the separate connector-runner (PR #16; recorded by #220) — proving the app-level path only; NOT per-tenant customer credentials, other providers, rotation/revocation, hosted KMS/IAM separation beyond that one secret, the connector data-sync, or production. | The vault is BUILT ([42](./42_CONNECTOR_CREDENTIAL_VAULT_DESIGN.md), realizing [19](./19_CONNECTOR_CREDENTIAL_VAULT_DESIGN.md)) — encrypted credential store, key separated from the app DB role, §8 tests green, `0029` grants hosted-verified (staging+prod), one staging app-level Slack client secret proven envelope-only (PR #16). **Still REQUIRED for closure, before any connector or real CUSTOMER secret and in production:** broader hosted KMS/IAM-separation verification for per-tenant customer connector credentials + other providers; rotation/revocation exercised end-to-end; the full connector path (first-real-token B2c exchange + decrypt/use **DONE on staging 2026-07-04**; rotation/revocation **DONE on staging 2026-07-06** - vault-version lifecycle; a provider-side token rotation was NOT forced, Slack re-issued the same token); and permanent deletion of the staging source secret after 2026-07-10. **Remains OPEN — partial implementation ≠ closure; closing RISK-007 and unblocking Phase C are separate explicit approvals.** |
| RISK-008 | P2 | open | **Imports/exports not built.** Legacy did destructive full-replace imports + unscoped exports. | Deferred; rules pre-committed (upsert + soft-delete + audit; tenant-scoped export) in [06](./06_BUILD_SEQUENCE.md). | Import/export PRs that follow those rules + tests. |
| RISK-009 | P2 | open | **Audit retention unresolved.** Deletes are blocked, so `audit_logs` has no purge/archival path and grows unbounded. | Append-only is correct for tamper-evidence ([02 §4](./02_SECURITY_AND_RLS.md)). | A partition/archival design (not row delete). |
| RISK-010 | P1 | open | **Legacy Firebase still production**, including its known P0s (plaintext creds, mutable logs, frontend authz). v3 does not fix the live system. | Tracked in [current-security-risk-map.md](./current-security-risk-map.md). | Legacy P0 patches (separate effort) and/or v3 cutover. |
| RISK-011 | P3 | open (narrowed PR #11) | **Supabase types could drift from the schema.** Typed `Database` (`src/lib/database.types.ts`) now exists and the server client is typed, but regeneration is **manual** — no CI check fails if `database.types.ts` falls behind a new migration. | Types are **generated** (not hand-edited) from the migrations via `scripts/gen-types-local.sh` (local-only, hosted-proof); the file header says "do not edit by hand"; the typed build would surface most column mismatches. | A CI drift check (regenerate in CI and `git diff --exit-code`) when it's worth the Docker cost. Not closed until that exists. |
| RISK-013 | P3 | open | **Vercel platform telemetry not privacy-reviewed for production.** Web Analytics (`@vercel/analytics`, PR #5) + Speed Insights (`@vercel/speed-insights`, PR #7) are `implemented` in the root layout. Low risk today (no production/custom domain points at v3), but unreviewed for real customer traffic. | Bare components only — **no custom events**, no PII/tenant/customer/business data sent; not used as an audit/product/billing/compliance source of truth; no production domain pointed at v3 ([01](./01_ARCHITECTURE.md#platform-telemetry-vercel)). | A production privacy/telemetry review (data sent, retention, DPA) **before** customer traffic. |

| RISK-014 | P2 | open | **Connected-agent automation could cause unintended repo/runtime changes.** Claude/Vercel/GitHub/Supabase agents can branch, edit, and open PRs; an over-broad action could push to `main`, auto-merge, alter secrets, run a hosted Supabase migration, change DNS, or promote production. | Branch-only writes; CI gates on every PR (`review-discipline` + `rls-tests` + `migration-safety`); **no auto-merge**; no hosted Supabase writes (local-only, separate runbook PR); no service-role keys; **human approval before merge**. Policy: [09 · Connected agent permissions](./09_AGENT_HANDOFF.md#connected-agent-permissions). | Branch protection on `main` requiring review + green CI (enforced setting), kept aligned with the documented policy. |

| RISK-015 | P3 | open | **Local/demo fixture misuse.** `supabase/fixtures/local_demo.sql` writes synthetic rows (incl. `auth.users`) and, if ever applied to hosted Supabase, would inject fake tenants/users/data into a real environment. | Lives outside `supabase/migrations/` (never in the apply path); `seed-local-demo.sh` is hosted-proof (own throwaway container, refuses remote/`--linked`, no Supabase CLI, no service-role, no secrets); strong LOCAL-ONLY header; all-synthetic data; `auth.users` inserts only work against a local shim. | Inherent local-tool risk; mitigations make accidental hosted application very unlikely. Revisit if a hosted seed/runbook is ever introduced. |

| RISK-016 | P2 | open · **gate hardened PR #36** | **v3 may miss legacy paid-client (OMC/Flywheel) capabilities if parity isn't tracked.** The legacy Firebase app has a large feature surface (inventory, contracts, people/matching, unmanaged/stale reports, license/spend, imports/exports, audit); cutting OMC over with gaps would regress the paying client. **OMC is a production replacement, NOT a pilot.** | The binding **go/no-go cutover gate** is now [17_OMC_PRODUCTION_REPLACEMENT_PARITY_GATE](./17_OMC_PRODUCTION_REPLACEMENT_PARITY_GATE.md) (PR #36 — grounded ~105-row replacement matrix from a real legacy inspection, hard-blocker list, go/no-go checklist, OMC-confirmation list, honest **~70–110-PR** estimate; **17 wins** if it disagrees with the [11](./11_LEGACY_PARITY_AND_OMC_CHECKLIST.md) scorecard or the [14](./14_LEGACY_UX_WORKFLOW_PARITY_MAP.md) UX contract). Every product-surface PR updates them + answers the [07](./07_P0_REVIEW_CHECKLIST.md) parity-approval question; legacy stays production until P0/P1 are `verified` + signed off. Evidence from the legacy repo, not memory. | Cutover sign-off once **every 17 §5 box** is true (all required workflows `Same`/`Better-approved`/`Removed-approved` + hosted-applied + verified + OMC signoff). **Remains OPEN; v3 is not production-replacement-ready.** |

| RISK-017 | P3 | open | **`next`'s bundled `postcss` has a moderate advisory** (GHSA-qx2v-qp2m-jg93 — XSS via unescaped `</style>` in CSS stringify). `npm audit` flags `node_modules/next/node_modules/postcss` (next-internal, build-time). Not safely fixable: the only `npm audit fix --force` path downgrades `next` to 9.3.3 (a breaking, ancient version). | Build-time only — the advisory needs postcss to stringify **attacker-controlled** CSS, which our build never does (it processes our own trusted CSS). No runtime/served exposure. The project's **own** top-level `postcss` is already 8.5.15 (patched); only `next`'s bundled 8.4.31 is affected. Not force-fixed (would break the app). | A `next` release > 16.3.0-canary.5 that bumps its bundled `postcss` ≥ 8.5.10; re-run `npm audit` and update on the next safe `next` upgrade. |

## Closed risks (verified)
| ID | Sev | Closed by | What it was |
|----|-----|-----------|-------------|
| RISK-C01 | P0 | PR #1 (`0002`/`0003`, tests T7/T22+23) | Cross-tenant org-pointer leak — a member could point a resource's owning-org at a foreign-tenant org and that org's members read/edited it. Closed by tenant-bound reads + `enforce_owning_org_tenant`; original exploit replayed and blocked. |
| RISK-C02 | P1 | PR #1 (`0002`, test T16) | Tenant-admin self-promotion to `owner` / owner demotion. Closed by owner/admin membership policy split. |
| RISK-C03 | P1 | PR #2 | RLS regressions could merge unnoticed. Closed by `test-rls.sh` + `rls-tests.yml` (full migration chain + the complete `org_rls_test.sql` assertion suite — currently 222 assertions, T1–T35 — on every PR). |
| RISK-C04 | P2 | PR #3 | Dangerous/disordered migrations could merge. Closed by `check-migration-safety.sh` + `migration-safety.yml`. |
| RISK-C05 | P2 | PR #4 | Documentation/risk drift invisible to reviewers. Mitigated by `check-docs-updated.sh` + `review-discipline.yml` + this register. |
| RISK-C06 | P1 | PR #6 | **Auth/session not built** (was RISK-005). Closed by the auth skeleton: `@supabase/ssr` user-scoped server client, `src/proxy.ts` session refresh + route guard, login/logout, protected route group. `check-auth-safety.sh` proves no service-role/no client-side role storage. *Caveat:* not yet exercised against hosted Supabase Auth (tracked by RISK-001). |
| RISK-C08 | P2 | PR #17 | **Cross-tenant child/link write corruption.** A child/link row could be written referencing a parent in a different tenant (e.g. an `app_users` row with `tenant_id = B` but `app_id` pointing at a tenant-A app). RLS hid such rows on read but did not prevent the write. Closed by `0005` (`UNIQUE (id, tenant_id)` on parents + composite same-tenant FKs on the child/link tables); proven by **T26** (11 cross-tenant inserts each rejected with `foreign_key_violation`, one per composite same-tenant FK path including the isolated `license_evaluations.license_rule_id` link; valid same-tenant + nullable links still insert). *Honest caveat:* org-scoped child-table **reads** remain deferred (RISK-002). |
| RISK-C07 | P2 | PR #16 | **Normal authenticated hard-delete of core evidence rows.** `0001`/`0002` `FOR ALL` manage policies silently granted `DELETE` on `organizations`/`apps`/`contracts`/`app_contracts`/`people`/`app_users`, so an editor/owner/admin/org-manager could destroy customer evidence before any archive UI or audit existed. Closed by `0004` (split into `INSERT`+`UPDATE`, no `DELETE` policy); proven by tests T17/T24/T25 (delete → 0 rows; reads/updates preserved). *Honest caveat:* archive/soft-delete UI is **not built** — a future audited admin break-glass path is deferred ([06](./06_BUILD_SEQUENCE.md)). The contract **write** model ([13_CONTRACT_STEWARD_WRITE_DESIGN](./13_CONTRACT_STEWARD_WRITE_DESIGN.md)) reaffirms `INSERT`/`UPDATE`-only with no `DELETE`/`FOR ALL`; the contract write **path** shipped in PR #30 (server-side DAL + actions) adds **only** create + update (no delete, no archive, no `FOR ALL`, no service-role) and exposes no hard-delete entry point — posture preserved. The contract create/edit **UI** (PR #31) and the **parity-fields** migration (PR #32 — `0011`, additive columns only; no policy/trigger change, no DELETE/`FOR ALL`) likewise add no delete/archive path. Archive/soft-delete stays a separate deferred design. RISK-002 + RISK-016 remain **open**. |

## How to use
- Opening a risk: add a row to **Active** with a real mitigation and a concrete "what closes it".
- Closing a risk: move it to **Closed** with the PR and the test/mechanism that proves closure — only if actually verified.
- Severity changes: edit in place and note it in [05_ENGINEERING_CHANGELOG.md](./05_ENGINEERING_CHANGELOG.md).
