# 09 · Agent Handoff

**Canonical source for: how a coding agent (Claude/Codex/etc.) safely continues work.**
Read this + [00_PRODUCT_STATUS](./00_PRODUCT_STATUS.md) before doing anything. Repo rules
also live in `AGENTS.md` / `claude/CLAUDE.md`.

## Current repo state (verify before trusting — see [00](./00_PRODUCT_STATUS.md))
Read-only governance foundation + the **contract write workflow** (PRs through #34 merged — create/edit UI + parity
fields `0011` + PDF/AI **design** [16] + `files` metadata foundation `0012`; **Partial** parity still. **#35 adds the
`files` RLS policies** — migration `0013`: SELECT `is_tenant_member` (tenant-member read; org-scoped deferred) + INSERT
`uploaded_by = auth.uid() AND can_write_contract(contract_id, tenant_id)` (the `0004` contract-write authority — tenant
editor+ OR procurement-org manager; **`paying_org` grants no write**); **no UPDATE/DELETE/FOR ALL**. `files` now has a
**tested read+write authorization model** but is **still NOT surfaced** — no Storage, upload, signed URLs, scan/AI, UI,
or app DAL touches `files`).
Migrations `0001`–`0013` are `implemented`, `verified-local`, `ci-enforced`, and now **`staged`** (applied to
staging `ycdpzduxugdsffjqyoai` — PR #47/#48; **not** production-applied; Storage object policies + verification
pending — RISK-001 partial/OPEN). Migration `0014` (contract-file Storage auth helpers — PR #51) is `verified-local` + `ci-enforced` but **not yet staged** (`org_rls_test.sql` = 222 assertions, T1–T35; 67 vitest tests). Auth/session skeleton, read-only
tenant/org context, and a typed DAL are built. **Product surfaces ship:** `/apps`
+ `/apps/[id]` (with app-user roster, match-status column, account-summary card), `/contracts` +
`/contracts/[id]`, and linked app↔contract panels — read-only, RLS-scoped — **plus** the contract
**create/edit** write surface (`/contracts/new`, `/contracts/[id]/edit`). **Not** exercised
against hosted Supabase. **Design-only:** identity matching read-scope (doc 12; match-status slice
built). **Contract PDF/AI extraction** (doc 16): **design (#33) + `files` schema (`0012`, #34) + `files` RLS (`0013`, #35)**
exist; the upload/Storage/signed-URL/scan/AI/UI surface does **not** — `files` is authorized-by-design + tested but **not
surfaced** (no app DAL/route/UI touches it). **Contract steward write** (doc 13): RLS
authority (`0004`) + audit (`0010`, PR #29) + write path (PR #30) + create/edit UI (PR #31) + parity fields (`0011`,
PR #32) all exist; **legacy parity is still Partial** (`commodity_*`/`validated`/gantt — gaps in [15]; PDF/AI surface is
**not built** — [16]). No contract delete/archive, no link/unlink, no file UI/upload/Storage/AI;
no tenant switching, no provisioning, no UAR, no hosted apply.
Vercel **Web Analytics + Speed Insights** are present (platform telemetry only, bare components). Legacy
Firebase is still production; OMC cutover + new paid-customer onboarding **blocked**. Don't trust any
prompt's "seeded" history — re-verify from `git log`, `gh pr list`, `ls supabase/migrations`, and the source/test files.

## Non-negotiable rules
- **OMC/Flywheel is a paying production REPLACEMENT, not a pilot.** Never say "pilot", "pilot-ready", or "MVP pilot"; never optimize for a minimal viable pilot. Optimize for production-replacement parity (no missing/broken workflows) per [17](./17_OMC_PRODUCTION_REPLACEMENT_PARITY_GATE.md). Do not introduce product improvements before replacement parity unless explicitly `better-approved` (17 §6). Do not claim cutover-readiness; cutover is a NO until every 17 §5 box is true.
- **Full OMC parity is required BY DEFAULT** ([27_LEGACY_OMC_FULL_PARITY_MATRIX](./27_LEGACY_OMC_FULL_PARITY_MATRIX.md) is the master line-item tracker). **Do not narrow OMC replacement to "OMC-critical workflows only"** unless the specific matrix row is explicitly `deprecated-approved` (owner + OMC signoff — never a developer assumption). **Future feature work must cite the relevant doc 27 row(s)** it advances, with evidence; "better than legacy" / "contracts done" does not satisfy a row. **Missing or unknown legacy behavior remains an OMC blocker until inventoried** (`blocked-unknown-legacy-behavior`) **or `deprecated-approved`.** Doc 27 implements nothing, proves no parity, and does **not** close RISK-001 or authorize cutover. **The ranked sequence of the remaining doc 17 §5 cutover blockers + the next 3 PRs is [30_DOC17_CUTOVER_BLOCKER_SEQUENCE](./30_DOC17_CUTOVER_BLOCKER_SEQUENCE.md)** — the Storage path is complete but **not sufficient** for cutover (1 of 17 boxes' boundary; 16 remain); RISK-001 stays OPEN; cutover BLOCKED. The next 3 PRs are hosted-staging Auth + full RLS-suite-on-hosted verification, then an OMC-shaped dataset + critical-flow validation plan, then a required-workflow parity build plan from doc 27 — not cutover. **Item #1 (Auth/tenant-context) is now RUN GREEN** — a human ran `scripts/verify-staging-auth-tenant-context.mjs` against the deployed staging app `https://idcaddie-v3.vercel.app` (after correcting + redeploying the Vercel `NEXT_PUBLIC_SUPABASE_*` env to staging): **8/8 automated + manual Tenant A/B passed**, real hosted Auth + user-scoped JWTs, no service-role, no production touched, no secrets — evidence in [31 §7](./31_HOSTED_STAGING_AUTH_TENANT_CONTEXT_VERIFICATION.md) (advances doc 17 §5 boxes 5/6/8; the full RLS-suite-on-hosted re-run + dataset/critical-flows remain). The synthetic-fixture cleanup is recorded ([31 §8](./31_HOSTED_STAGING_AUTH_TENANT_CONTEXT_VERIFICATION.md)): tenant/org access removed (memberships=0); **2 profiles + 2 tenants + 2 Auth users retained as audit anchors** because `audit_logs` is append-only (`reject_audit_mutation()`, `0002`) — immutability working as intended; the retained users have no tenant/org access. **The full `org_rls_test.sql` re-run against hosted (remaining part of boxes 5/8) is PREPARED, not yet run** — `scripts/verify-staging-rls-suite.mjs` ([30 §6](./30_DOC17_CUTOVER_BLOCKER_SEQUENCE.md)) is a ref-guarded refusal gate: the raw suite `TRUNCATE`s 17 tables (incl. append-only `audit_logs`, bypassing the row-level trigger) + `delete from auth.users`, so it must **never** run against the shared staging project even rollback-only; the gate refuses, connects to nothing, and only an explicit disposable-isolated opt-in **emits a rollback-only runbook** (count-snapshot → `begin…rollback` → verify counts unchanged → dispose) for a human to run against a separate disposable project — no connection string handled, no secrets printed. **Item #2 is PREPARED (not executed):** item #2 (OMC-shaped dataset + critical-flow validation) = the runbook [32_STAGING_OMC_SHAPED_DATASET_AND_CRITICAL_FLOW_VALIDATION](./32_STAGING_OMC_SHAPED_DATASET_AND_CRITICAL_FLOW_VALIDATION.md) (dataset definition + validation plan + a review-and-apply SQL template — **runbook only, no committed runnable seed; not-built flows are blockers, not failures**). Both are staging-only, synthetic, no secrets; the hosted run + the manual UI steps + the synthetic-user/dataset setup are human-executed later. **An agent never runs the hosted verifier, seeds staging, or creates hosted users.** **Item #3 (required-workflow parity build plan) is now PREPARED** — the ranked, buildable plan (9 implementation tracks, P0/P1/P2, P0 detail, built-but-unverified-vs-not-built, and the next 3 implementation PRs) is in [33_REQUIRED_WORKFLOW_PARITY_BUILD_PLAN](./33_REQUIRED_WORKFLOW_PARITY_BUILD_PLAN.md). The next 3 **implementation** PRs (contract-file upload action + signed-URL read; files list/detail/preview; contract field-parity + link/unlink write) build on the verified Storage boundary **after** items #1/#2 are executed green; each must cite its doc 27 row(s), carry RLS tests + hosted staging validation + evidence, and tick no §5 box on its own. **Item #4 (OMC legacy → v3 data migration plan) is now PREPARED** — [34_OMC_LEGACY_DATA_MIGRATION_PLAN](./34_OMC_LEGACY_DATA_MIGRATION_PLAN.md) (sources→targets, blocked-until-built, 8 phases, reconciliation incl. file byte/`sha256`, non-destructive rules, security/privacy, named tooling PRs, evidence). **Planning only — no migration tooling/run; migrate only built+verified surfaces; never migrate connector secrets (RISK-007) or via `local_demo.sql` (RISK-015); never service-role on a request path; no real OMC data in the repo.** An agent never runs a migration, exports real OMC data, or seeds hosted data. **Item #5 (cutover rollback rehearsal plan) is now PREPARED** — [35_CUTOVER_ROLLBACK_REHEARSAL_PLAN](./35_CUTOVER_ROLLBACK_REHEARSAL_PLAN.md) (rollback definition, 8 domains, 6 staging rehearsal phases, 7 production triggers, hard-stop rules + named decision owners + PONR, box-15 evidence). **Planning only — no rollback rehearsed/run; no DNS/Vercel/Auth/Storage/DB change.** An agent never executes rollback, touches DNS/Vercel, or mutates hosted systems. **Item #6 (OMC acceptance/signoff plan) is now PREPARED** — [36_OMC_ACCEPTANCE_SIGNOFF_PLAN](./36_OMC_ACCEPTANCE_SIGNOFF_PLAN.md) (acceptance definition, 8 signoff domains, signers-by-role, evidence package, 4 outcomes, approved-removal recording via the doc 27/17 taxonomy, evidence format, hard rules). **All six doc 17 blocker-sequence items (#1–#6) now exist as PREPARED plans — none executed.** What remains is the real work: execute the hosted Auth/dataset verifications (items #1/#2), build the required workflows (item #3 plan), run the migration + reconciliation (item #4), rehearse rollback (item #5), and record OMC acceptance (item #6) — each a human-executed step. **No signoff is recorded; engineering never self-accepts customer readiness; an agent never records a signoff, runs a hosted verification, or executes cutover.** RISK-001 stays OPEN; cutover BLOCKED until every doc 17 §5 box is true. **A pre-plan stocktake of all parity/cutover/readiness docs is in [37_EXISTING_PARITY_DOCS_AUDIT](./37_EXISTING_PARITY_DOCS_AUDIT.md)** — read it before authoring another parity plan. It separates **completed evidence** (staging+prod Storage REST 14/14; hosted Auth+tenant-context PASSED; Auth cleanup; the RLS unsafe-run gate) from **planned-not-executed** (docs 32/33/34/35/36 + the doc 18 confirmation pass + doc 19 vault + doc 16 PDF/AI), maps **missing/under-specified** parity areas to the legacy feature list, flags **docs drift** (parity is tracked in 5 places → 27/17/33 canonical; v3-product-scope's MVP subset is superseded for cutover by 17/27), and names the **highest-value next step: run the doc 18 OMC confirmation pass** to resolve the 40 `blocked-unknown` + `probably`/`unknown` rows and *size* the remaining build (~25 vs ~110 PRs). **Old-app parity / UI/UX / AI-API-connector parity are NOT complete; hosted Auth/tenant-context is verified, but old-app replacement is not yet verified.** **DECISION OF RECORD — [38_OMC_FULL_PARITY_SCOPE_DECISION](./38_OMC_FULL_PARITY_SCOPE_DECISION.md): cutover scope = FULL old-app parity. OMC requires full old-app parity before cutover unless OMC explicitly waives a specific capability in writing** (recorded as `removed-approved`/`deprecated-approved`/`not-used-by-OMC` in doc 27 with OMC signoff). **The MVP subset framing (`v3-product-scope`) is NOT sufficient for cutover** (annotated superseded-for-cutover; kept as history). AI, API/SaaS connectors, imports, exports, reporting, dashboards, old-app UI/UX, and all critical old-app workflows are **in scope** (cutover blockers) unless OMC-waived — **no implicit deferrals**. Realistic sizing: **full parity likely means dozens of PRs** (not the ~25–40 MVP figure); exact count needs direct old-app inspection + the doc 18 confirmation pass. **The direct-inspection packet is [39_OLD_APP_DIRECT_INSPECTION_INVENTORY](./39_OLD_APP_DIRECT_INSPECTION_INVENTORY.md)** — a 14-field per-screen capture template + a 30-category inventory (seeded with the real legacy `frontend-v2/` routes) + capture instructions + an OMC interview script + a gap ledger (the future build backlog) + a cannot-answer-from-repo list. **The old app is NOT in this repo, so the packet is PREPARED but nothing was inspected live — direct old-app inspection is still required; capture no secrets/tokens/real customer data.** **A CODE-DERIVED inventory (from the uploaded legacy codebase at `~/Desktop/IDCaddie_Repo-main/`: `frontend-v2/`, `webapp/functions/`, `extension/`, `DemoFeatures/`) is [40_CODE_DERIVED_OLD_APP_INVENTORY](./40_CODE_DERIVED_OLD_APP_INVENTORY.md)** — 50+ frontend routes, backend functions by trigger, **52 connectors/scrapers** (+ IDCIngestor; vault prerequisite RISK-007), AI Doc-processing (contract+invoice), 7 report types, and admin/security (SSO/SAML/OIDC, SCIM, groups+granular permissions, billing, API keys/tokens, audit incl. 90-day purge), each compared to v3 from **repo evidence only** (absent ⇒ Missing). **Derived from code, NOT live inspection — live old-app inspection remains incomplete; no screenshots/acceptance.** The code reveals a far larger app than prior docs implied (52 connectors + full SSO/SCIM + dashboards builder + 7 reports + billing + Chrome extension) — confirming **full parity likely means dozens of PRs** (the 52 connectors alone are a sub-program). **The implementation ROADMAP is [41_FULL_PARITY_IMPLEMENTATION_ROADMAP](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)** — 27 dependency-ordered epics (E01 shell → E27 acceptance/doc-17-closure) with per-epic scope/security/deps/PR-range/acceptance; connector waves (identity→collaboration→CRM→developer/cloud→finance/import→long-tail); a **critical path** (E16 vault → E17 framework → E18 connectors is the connector chokepoint; serial cutover tail E25 migration → E26 rollback → E27 acceptance); a **do-not-build-first** list (no connector before the vault — RISK-007; no reports before data; no migration before targets; no AI auto-apply; no service-role on request paths); **PR ranges** (min ~35–55 / realistic ~80–140 / worst ~180–260+); and a **next-10-PRs** sequence (hosted RLS run → files upload+surface → connector vault → people/matching → contracts field-parity → apps → dashboard → dataset validation → connector framework+Wave 1). **Roadmap only — implements no parity; build PRs each cite doc 27 rows + RLS tests + hosted validation + evidence and tick no §5 box.** **FIRST build PR shipped — PR #76 (E09a): contract-file attachment UI on `/contracts/[id]`** (list/upload/open) on the **verified Storage boundary**, reusing the `files` `0012`–`0015` foundation + the private bucket + RLS/Storage policies + `pdf-validation.ts` — **no migration/schema change, no service-role on request paths, no public bucket**. DAL `src/lib/data/contract-files.ts` (files-row-first RLS INSERT, server-derived `tenant_id`/path, 60 s signed URL only after the RLS read, DTO never leaks `storage_path`/URLs); cross-tenant denial already proven by `org_rls_test.sql` T34 + the hosted REST verifier 14/14; +9 app-layer tests. **Contract-file upload/download UI is partially implemented; Storage authorization remains necessary but not sufficient for cutover; upload is not automatically production-ready; no doc 17 box ticked; RISK-001 stays OPEN; cutover BLOCKED.** Pattern for future build PRs: user-scoped client only, RLS is the authority, validate from server-measured bytes, return safe error labels (never raw Supabase errors/paths/URLs). **PR #77 recorded the manual STAGING VERIFICATION of E09a ([41 §11](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md))** — a human verified the Tenant A happy path on `https://idcaddie-v3.vercel.app` (synthetic `tenant-editor-a@idcaddie-staging.local`, contract `cccca111-…a1`): contract detail loaded, the attachment section rendered, PDF upload worked, the file listed, and Open was available; the UI exposed no `storage_path`/signed-URL text. **Recorded honestly: multiple synthetic-test.pdf `pending` rows are visible (repeated upload during testing; no UPDATE/DELETE on the request path, so the app can't remove them; no cleanup performed — a future worker/admin reconciles).** This verifies ONLY the Tenant A attachment happy path on staging — not parity, AI, invoices, or connectors; it does not close RISK-001 or approve cutover. **Storage authorization remains necessary but not sufficient for cutover; upload is not automatically production-ready.** **PR #78 HARDENED upload finalization** ([41 §12](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)) after that staging finding (3 `files` rows for the contract, 1 orphan with no object, all stuck `pending`): the existing schema had no UPDATE policy, so PR #76 could not flip a row off `pending`. **Migration `0016`** adds a NARROW uploader-finalize UPDATE policy (`uploaded_by = auth.uid() AND can_write_contract`; WITH CHECK no-reassign) AND a **privilege correction**: `revoke update, delete, truncate on public.files from authenticated` then `grant update (upload_status)` — after it `authenticated` holds EXACTLY `SELECT, INSERT, UPDATE(upload_status)` (no DELETE, no TRUNCATE). The DAL now finalizes success → `upload_status='uploaded'` and dispositions a failed object upload → `failed`; download only signs finalized rows; the UI shows Open only for finalized files. **Successful uploads no longer remain ambiguous pending rows; failed uploads are explicitly dispositioned. No DELETE/`FOR ALL`, no Storage-policy change, no public bucket, no service-role.** **CAUGHT BEFORE MERGE: staging verification found the first cut left `authenticated` with BROAD DELETE/TRUNCATE/UPDATE on `public.files`** (a `grant update (col)` is additive — it never revokes; and `test-rls.sh`'s blanket grant had MASKED it). The corrected `0016` revokes the broad privileges; the harness now re-asserts the migration-intended `files` grants; **T37** proves no DELETE/TRUNCATE + UPDATE only on `upload_status` (suite 222→**248**); a human must re-apply the corrected privilege SQL to staging (not done here). **Future-build lessons: (1) prefer `revoke`+`grant` for privilege CHANGES — `grant (col)` is additive and never removes a broad grant; (2) the local harness's blanket grant can MASK over-broad table privileges — assert the privilege surface (`has_table_privilege`/`has_column_privilege`) for security-critical tables; (3) a feature blocked by a deliberate prior policy choice may justify a NARROW, tested migration, but reconcile stale catalog assertions (T33/T34).** **PR #79 recorded PR #78's STAGING VERIFICATION** ([41 §14](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): a human (a) repaired the staging `public.files` privileges (verified: **authenticated no longer has DELETE or TRUNCATE on `public.files` in staging**; SELECT/INSERT=true; **UPDATE only on `upload_status`**; the 3 policies are members-read/writers-insert/uploader-finalizes-own; production untouched), then (b) verified a new upload on `https://idcaddie-v3.vercel.app/contracts/cccca1cc-…cc` — **finalized `upload_status='uploaded'`, `storage_bucket='contract-files'`, `content_type='application/pdf'`, `byte_size=53826`, `has_storage_object=true`** (scan/extraction correctly stay pending/not_started). **New uploads now finalize; existing pre-fix synthetic-test.pdf pending/orphan rows remain as historical staging evidence; no cleanup was performed.** Verifies only the new finalization path on staging — not invoices/AI/parity; does not close RISK-001 or approve cutover. **Storage authorization remains necessary but not sufficient for cutover; upload is not automatically production-ready.** **PR #80 recorded a BLOCKED cleanup attempt** ([41 §15](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): a human tried to delete the old pre-fix synthetic-test.pdf rows by deleting from `storage.objects` directly + the matching `public.files` rows — **Supabase Storage protections safely blocked it** (*"Direct deletion from storage tables is not allowed. Use the Storage API instead."*). **No cleanup was performed; the pre-fix rows remain as historical staging evidence** (1 good finalized upload + 4 old `pending` rows, 2 with objects + 2 orphans). **Direct `storage.objects` DELETE is NOT an acceptable cleanup path — future cleanup must use an approved Storage API/admin/worker path** (the request path has no DELETE policy by design). The finalized post-PR #78 upload remains valid and openable. **PR #81 improved the authenticated SHELL/NAV (roadmap epic E01 — UI only, no backend)** ([41 §16](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): a persistent sidebar (`(authenticated)/nav.tsx` + pure `nav-items.ts` wired into `layout.tsx`) with full-parity nav groups, **active state**, and the signed-in email + active tenant name/role (**never the tenant id**) + sign-out. **Only the 3 implemented routes (`/`, `/apps`, `/contracts`) are linkable; every unbuilt old-app area is a disabled "Not built yet" item — no placeholder routes, no implication that unbuilt modules work** (a test fences that only real routes are linkable). No migration/policy/data-model/DB-write/hosted change; session guard unchanged; no secrets/IDs/tokens exposed; types 0-diff; suite 248; tests 77→83. **Core shell/navigation UI parity is improved but not complete; Old-app/UI-UX/AI-API-connector parity NOT complete; RISK-001 stays OPEN; cutover BLOCKED.** Pattern for UI parity PRs: extend the shell + mark unbuilt areas "Not built yet" (disabled), never fake access; keep the nav config pure + tested. **PR #82 recorded PR #81's STAGING VERIFICATION** ([41 §17](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): a human verified the shell/nav on `https://idcaddie-v3.vercel.app` as the synthetic Tenant A editor — **PASSED** (sidebar visible with signed-in email + active tenant name/role; Home active state; Apps/Contracts enabled; unbuilt areas marked "Not built yet"; nav groups Workspace/Applications/Contracts & Files/People & Identity/Insights/Administration; sign-out visible; **no signed URLs/storage paths/tokens/secrets/JWTs/cookies/service-role in the sidebar**). **Follow-up (honest): the nav chrome doesn't expose tenant IDs, but the Home page main content still displays the raw tenant slug + UUID from the older skeleton/debug content — clean it up in a future UI-polish PR.** Verifies only the shell/nav staging behavior — not full UI/UX or old-app parity; does not close RISK-001 or approve cutover. **Core shell/navigation UI parity is improved but not complete.** **PR #83 improved APPS INVENTORY/DETAIL parity (E03/E04 — read-only, existing RLS surfaces)** ([41 §18](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): `/apps` now shows per-app **RLS-scoped linked-contract + app-user counts** (new server DAL `listAppsWithCountsForCurrentUser()` — visible-to-you tallies, no service-role, no embedded joins); `/apps/[id]` adds a clear **"Not built yet" Actions** section (link/unlink, edit/archive, connector sync, AI analysis, export). No migration/policy/RLS change; no writes; no cross-tenant (RLS-scoped, T25/T28/T29); DTO carries no tenant id/secret/token (tested); types 0-diff; suite 248; tests 83→87. **Apps inventory/detail parity is improved but not complete; App write/edit/delete workflows remain not built; Connector sync remains not built; AI app/license intelligence remains not built; Old-app/UI-UX/AI-API-connector parity NOT complete; RISK-001 stays OPEN; cutover BLOCKED.** Pattern for read-only parity PRs: build on the existing RLS-backed DALs (apps/links/app-users/matches/account-intel), tally/aggregate in app code (never a new policy or service-role), keep DTOs minimal + tested, and mark unbuilt actions "Not built yet". **PR #84 recorded the APPS POPULATED-PATH staging verification** ([41 §19](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): a human applied a tiny synthetic, idempotent, staging-only Tenant A fixture (1 app + 1 link to an existing contract + 2 app_users + 1 person + 1 match; `5a9a0000-…` namespace, "Staging Apps Verification" names; no migration, no policy change, no Storage) and verified `/apps` (app row + linked-contract count 1 + app-user count 2, RLS-scoped not absolute) and `/apps/[id]` (summary, linked contracts, roster with one matched/one unmatched, "Not built yet" Actions) — **no tenant IDs/tokens/URLs/secrets visible**. The synthetic rows are **left in staging as verification fixtures** (staging-only, not customer data); the reviewed fixture SQL is documented in §19 (not a committed runnable script). **The agent ran nothing — no hosted command, no staging mutation; an agent still never seeds staging or runs hosted commands** (a human did, ref-confirmed `ycdpzduxugdsffjqyoai`, production untouched). Apps inventory/detail parity is improved but not complete; **App write/edit/delete + connector sync + AI intelligence remain not built; RISK-001 stays OPEN; cutover BLOCKED.** **PR #85 added read-only PEOPLE/IDENTITY parity (E05/E06)** ([41 §20](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): a `/people` route + DAL `listIdentityAccountsForCurrentUser()` showing the identity ACCOUNTS the user may read (app_users across visible apps, each with app link + account fields + matched/unmatched status + a summary). **Critical posture: it reads `app_users`/`apps`/`matches` ONLY — never `people`/`identity_accounts` — so NO person PII / no `person_id`; the org-scoped people directory read stays deferred (RISK-002).** People/Users nav now enabled → `/people`; Identity matching stays "Not built yet" (read-only status only; resolution workflow not built). No migration/policy/RLS change; no writes; no cross-tenant (T29/T30); DTO has no tenant/person id (tested); auth guard preserved; types 0-diff; suite 248; tests 87→92 (incl. a nav test that **every enabled nav item maps to an implemented route**). **People/Users read-only parity improved but not complete; manual/bulk matching + SCIM/IdP import + connector sync + AI matching + exports remain not built; RISK-001 stays OPEN; cutover BLOCKED.** Pattern for sensitive parity areas: surface only the already-reviewed RLS surfaces (app_users/matches), never the deferred directory, keep it read-only + flat (no implied resolution), DTO minimal + tested. **PR #86 CORRECTED the PR #84 populated-Apps staging record** ([41 §19 banner + §19.5](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): new live staging evidence shows `/apps` **empty ("No apps to show")** for `tenant-editor-a@idcaddie-staging.local` (tenant Storage Verifier Tenant A · editor), so **the PR #84 populated-path verification is superseded/corrected — NOT deleted** (the original record is retained with a banner). **What is true: PR #83 code merged + local validation passed; the `/apps` empty-state staging verification passed; Apps inventory/detail parity is improved IN CODE but populated hosted verification remains incomplete.** **NOT currently verified: populated `/apps` (still shows "No apps to show" for this account), `/apps/[id]` populated detail, and `/people` populated path.** **The synthetic fixture is either not present, not visible through RLS, or not associated with this account/tenant — exact root cause UNVERIFIED** (this corrective PR ran no hosted query). **No production touched; no hosted commands run in this corrective PR; no RLS-policy/migration change; old-app parity not complete; RISK-001 stays OPEN; cutover BLOCKED.** **Lesson: a populated-path "verification passed" recorded from provided facts must be reproducible against live staging for the named account — re-verify populated paths live before treating them as fact.** **PR #87 added a repeatable, GUARDED, human-run staging fixture PROCESS for Apps/People populated-path verification** ([41 §21](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): a reviewed fixture `supabase/fixtures/staging_apps_people_verification.sql` (1 app + 1 link to existing contract `cccca111-…a1` + 2 app_users + 1 person + 1 match; `5a9a0000-…` synthetic namespace; INSERT-only/idempotent; no Storage/RLS/migration) + a launcher `scripts/seed-staging-apps-fixture.sh` that **fails closed unless the linked ref is exactly `ycdpzduxugdsffjqyoai`, prints the ref, refuses `dzbfxulvxchdemcettrx` explicitly, and requires the phrase `SEED STAGING APPS FIXTURE`** (no service-role; default print-only/no-connection; optional `psql` only if the human's `STAGING_DB_URL` references the staging ref; never runs in CI/tests; not production-safe). +6 local guard tests (98 total), no hosted call. **The process is human-run and was not executed by the agent; the current live populated-path verification remains incomplete until a human applies the fixture and verifies `/apps`, `/apps/[id]`, and `/people`.** §21 documents purpose/tables/IDs/command/per-route checklist/cleanup/warnings. **No production touched; no hosted commands run by the agent; no RLS-policy/migration change; Apps + People code parity improved but populated hosted verification incomplete; RISK-001 stays OPEN; cutover BLOCKED.** Pattern for future hosted-fixture needs: commit a reviewed synthetic fixture + a fail-closed (ref-guard + confirmation-phrase) launcher with a guard test; the agent authors + tests it but a human runs it. **PR #89 RECORDED the Apps/People POPULATED-PATH staging verification — it now PASSES** ([41 §23](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md); resolves the §19/§19.5/§21 chain, with §19.5's #86 "not verified" correction marked RESOLVED, not erased): a human applied the §21 fixture to staging `ycdpzduxugdsffjqyoai` and verified the three live routes as `tenant-editor-a@idcaddie-staging.local`. **Apps + People populated-path staging verification PASSED** — `/apps` shows the synthetic app with RLS-scoped Contracts 1 / Users 2; `/apps/[id]` shows the linked contract + two app users + one matched + one unmatched + "Not built yet" actions; `/people` shows two app-user accounts (one matched, one unmatched) without person/IdP directory details. **Counts are RLS-scoped / visible-to-you, not absolute totals.** This confirms the §86 "No apps to show" was the fixture not yet applied/visible to that account (now resolved) — NOT a code bug; the separate PR #88 inventory hardening (§22, still ensures a count-read failure never erases readable apps) is unrelated to this pass. **The agent ran nothing — no hosted command, no staging mutation; production untouched. The fixture remains synthetic (not customer data); this does not close RISK-001 or approve cutover. Manual/bulk matching + SCIM/IdP import + connector sync + AI matching + exports + people directory + UAR remain not built; Old-app/UI-UX/AI-API-connector parity NOT complete; RISK-001 stays OPEN; cutover BLOCKED.** **PR #90 added read-only REPORTS + AUDIT/LOGS parity (E14/E23)** ([41 §24](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): a `/reports` route (DAL `getReportsSummaryForCurrentUser()` — RLS-scoped "visible to you" summary counts: apps/contracts/files via `head:true` exact counts + accounts/matched/unmatched via the people helper; each `number|null`/"—", DTO numbers-only) and a `/audit` route (DAL `listRecentAuditEntriesForCurrentUser()` — recent ≤50 `audit_logs` entries). **Audit posture: `audit_logs` already has a safe tenant-member SELECT (`is_tenant_member`, `0001`) + is append-only (`reject_audit_mutation`, `0002`), so NO policy change; the DTO is deliberately minimal (action/resourceType/createdAt/"actor recorded" boolean) and NEVER exposes `tenant_id`, raw `actor_user_id`, `resource_id`, `ip_address`, `user_agent`, or the `before_json`/`after_json` diff blobs (asserted by a test).** Reports/Audit nav now enabled → `/reports`, `/audit`. No migration/policy/RLS change; no writes; no service-role; no exports; types 0-diff; suite 248; tests 98→104. **Reports + Audit/Logs read-only parity improved but not complete; exports/scheduled/CSV-PDF/AI insights/connector-driven reporting + before-after diff + audit mutation/delete remain not built; RISK-001 stays OPEN; cutover BLOCKED.** Pattern for audit/log surfaces: project to a deliberately-minimal DTO (never the diff JSON / IP / UA / tenant id), cap rows (no broad export), rely on the existing append-only + tenant-member RLS — never add an unsafe read policy. **PR #91 RECORDED the PR #90 Reports + Audit/Logs STAGING VERIFICATION — both PASS** ([41 §25](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): a human verified `https://idcaddie-v3.vercel.app` as `tenant-editor-a@idcaddie-staging.local`. **`/reports`** (nav enabled, deployed) shows RLS-scoped visible-to-you counts Apps 1 / Contracts 2 / accounts 2 / matched 1 / unmatched 1 / Files 5 (stated not absolute totals); exports/scheduled/CSV-PDF/AI/connector/legacy-report-types all "Not built yet". **`/audit`** (nav enabled, deployed) shows 2 recent entries with **action/entity/timestamp + a safe actor indicator only** (e.g. `contract.created / contract / 2026-06-18 12:10`) — **NO tenant_id / actor_user_id / IP / user_agent / before_json / after_json / tokens / secrets / signed URLs / storage paths / connector creds / JWTs / cookies / service-role**; page states append-only; before-after diff / search-filter-export / retention-purge / mutation-delete all not built. **The agent ran nothing — no hosted command, no staging mutation; production untouched. RISK-001 stays OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #92 added read-only ADMIN / SETTINGS parity (E21)** ([41 §26](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): a `/admin` route that **reuses the existing RLS-scoped `resolveTenantContext()` — NO new DAL, no service-role, no new DB read** — via a pure mapper `toAdminContextView()` exposing ONLY signed-in email + active tenant NAME/role + org membership NAMES/roles + an "implemented modules" overview. **No raw tenant/org/user id is exposed on the page** (the mapper drops every id; a test asserts the projected view contains none of the source ids — the home/debug page keeps the tenant id, this page does not). Admin/Settings nav now enabled → `/admin`. Read-only: tenant switching / invitations / role mgmt / SSO-SAML-OIDC / SCIM-IdP / connector vault / billing / API keys / retention / security settings all "Not built yet". No migration/policy/RLS change; no writes; auth guard inherited; types 0-diff; suite 248; tests 104→108. **Admin/Settings read-only parity improved but not complete; all admin write/management capabilities remain not built; RISK-001 stays OPEN; cutover BLOCKED.** Pattern for context/admin surfaces: reuse the resolver + a pure id-dropping mapper (tested for no-id-leak), never a new policy/service-role, never expose raw ids on a non-debug page. **PR #93 RECORDED the PR #92 Admin / Settings STAGING VERIFICATION — PASSED** ([41 §27](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): a human verified `https://idcaddie-v3.vercel.app/admin` as `tenant-editor-a@idcaddie-staging.local` (tenant Storage Verifier Tenant A) — nav enabled + route deployed; the page shows **signed-in email, active tenant name, role, tenant membership count, organization membership summary, implemented modules, and explicit Not built yet administration capabilities**; **raw tenant IDs and raw organization IDs are intentionally NOT shown**; tenant switching / invitations / role mgmt / SSO-SAML-OIDC / SCIM-IdP / connector vault / billing / API keys / retention / security settings all "Not built yet"; **no tokens/secrets/API keys/connector creds/JWTs/cookies/signed URLs/storage paths/service-role visible**. **The agent ran nothing — no hosted command, no staging mutation; production untouched. RISK-001 stays OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #94 added read-only FILES / DOCUMENTS parity (E09)** ([41 §28](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): a `/files` route + DAL `listFilesForCurrentUser()` listing the contract files the user may read (RLS-scoped — `files` SELECT = `is_tenant_member`, `0013`) with safe metadata (file name, related contract link, upload status, content type, size, added date). **Reuses the contract-files discipline: user-scoped client, explicit safe column subset, DTO that NEVER selects/exposes `storage_path`, `storage_bucket`, the raw object name, `sha256`, `tenant_id`, `uploaded_by`, the extraction blobs, or any signed URL (asserted by a test).** Files/Documents nav now enabled → `/files`. **Standalone open/download remains not built (the existing signed-URL helper is NOT reused standalone — keeping `/files` signed-URL-free; the file links to its contract, where the verified open flow lives); standalone upload/delete/export + connector ingestion + AI doc analysis remain not built; contract-level attachment remains the implemented upload path.** No migration/policy/RLS change; no writes; contract-files.ts + contract detail untouched (existing behavior preserved); auth guard inherited; types 0-diff; suite 248; tests 108→114. **Files/Documents read-only parity improved but not complete; RISK-001 stays OPEN; cutover BLOCKED.** Pattern for file/storage surfaces: reuse the contract-files safe-column-subset DTO, never select storage_path/bucket/sha256, never put a signed URL on a list page — link to the contract's verified open flow instead. **PR #95 RECORDED the PR #94 Files / Documents STAGING VERIFICATION — PASSED** ([41 §29](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): a human verified `https://idcaddie-v3.vercel.app/files` as `tenant-editor-a@idcaddie-staging.local` (tenant Storage Verifier Tenant A) — nav enabled + route deployed; **5 files visible** with file names, related contract links, upload status, content type, size, added dates; the uploaded PDF row (`Invoices from Insight Canada Inc (3).PDF` · Storage Test Contract A Central · Uploaded · application/pdf · 52.6 KB · 2026-06-20) is visible + linked; pending synthetic-test.pdf rows marked "Pending — not yet openable"; the page instructs opening files from the related contract (verified open path); **NO storage paths / object names / signed URLs / bucket internals / tenant IDs / tokens / secrets / service-role / JWTs / cookies / connector creds / API keys visible**; standalone upload/open-download/delete/export + connector ingestion + AI doc analysis all "Not built yet"; contract-level attachment remains the implemented upload/open path. **The agent ran nothing — no hosted command, no staging mutation; production untouched. RISK-001 stays OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #96 added read-only DASHBOARDS parity (E02)** ([41 §30](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): a `/dashboards` route + a thin DAL `getDashboardSummaryForCurrentUser()` that **COMPOSES two already-verified helpers** — `getReportsSummaryForCurrentUser()` + `listRecentAuditEntriesForCurrentUser()` (count only) — into a **numbers-only** summary (NO new DB read, no service-role). Stat cards (apps/contracts/files/accounts+matched-unmatched/recent-audit/reports) link ONLY to implemented pages (`/apps`,`/contracts`,`/files`,`/people`,`/audit`,`/reports`); **counts are RLS-scoped / visible-to-you, not absolute totals**; DTO is integers/nulls only (no tenant/org/actor id, storage path, signed URL, audit JSON, IP/UA — tested). Dashboards nav now enabled → `/dashboards`. **Custom dashboard builder / charts / connector-spend-license dashboards / AI insights / export / scheduled delivery all remain not built.** No migration/policy/RLS change; no writes; reused helpers unchanged (pure reuse); auth guard inherited; types 0-diff; suite 248; tests 114→117. **Dashboards read-only parity improved but not complete; RISK-001 stays OPEN; cutover BLOCKED.** Pattern for summary/overview surfaces: COMPOSE existing verified RLS-scoped helpers into a numbers-only DTO, link only to implemented routes, never add a new query/policy/service-role. **PR #97 RECORDED the PR #96 Dashboards STAGING VERIFICATION — PASSED** ([41 §31](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): a human verified `https://idcaddie-v3.vercel.app/dashboards` as `tenant-editor-a@idcaddie-staging.local` (tenant Storage Verifier Tenant A) — nav enabled + route deployed; counts **RLS-scoped / visible-to-you, not absolute totals** (Apps 1 / Contracts 2 / Files 5 / accounts 2 / 1 matched / 1 unmatched / Recent audit entries 2; Reports card visible); **cards link only to implemented pages**; **recent audit entries are count-only — no audit detail, actor identity, IP, or raw audit data**; **NO tenant IDs / actor IDs / raw audit JSON / storage paths / signed URLs / tokens / secrets / service-role / connector creds / JWTs / cookies / API keys visible**; custom dashboard builder / charts / connector-spend-license dashboards / AI insights / export / scheduled delivery all "Not built yet". **The agent ran nothing — no hosted command, no staging mutation; production untouched. RISK-001 stays OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #98 CHECKPOINT — Read-only visible parity wave through PR #97 is complete and staging-verified** ([41 §32](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)): shell/nav, contract files, Files/Documents, Apps inventory + detail, People/Users, Reports, Audit/Logs, Admin/Settings, Dashboards — each read-only, RLS-scoped, human-verified on staging `ycdpzduxugdsffjqyoai` (production `dzbfxulvxchdemcettrx` never touched); after #97 RLS 248 / 117 tests / 16 routes; the synthetic Apps/People fixture process exists + was human-run. **This is not full old-app parity. This is not cutover readiness — a milestone, not cutover approval.** **Connectors / AI / Analysis / identity matching workflow / imports / exports / billing / SSO-SAML-OIDC / SCIM-IdP import / connector credential vault remain not built** (plus tenant switching, invitations, role mgmt, API keys/ingestion tokens, connector-driven spend/license reporting, real report builder/CSV/PDF/scheduled delivery, AI intelligence, production cutover). **The next strategic fork is connector vault versus identity matching workflow** — do NOT start connectors/AI/imports/exports directly; do EITHER (A) a **connector credential vault security design PR** (secrets/encryption/rotation/audit/least-privilege/no-browser-exposure; higher risk), OR (B) an **identity matching workflow RLS/write design PR** (manual match/unmatch, audit, reversible, no cross-tenant leakage; medium-high risk). **No production data was touched. No hosted commands were run by the agent. No RLS policies were changed. No migrations were added. RISK-001 stays OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #99 DRAFTED the Option-A CONNECTOR CREDENTIAL VAULT SECURITY DESIGN** ([42_CONNECTOR_CREDENTIAL_VAULT_DESIGN](./42_CONNECTOR_CREDENTIAL_VAULT_DESIGN.md); [41 §33](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)) — docs-only, the prerequisite for any connector work + RISK-007. **Connector credential vault design is drafted but not implemented; connector implementation remains blocked until the vault design is reviewed and accepted.** Five load-bearing decisions: (1) two-tier split — RLS-readable metadata vs a secret store with NO `authenticated` grant + RLS deny-all (no SQL a logged-in user runs returns a secret); (2) envelope encryption (per-secret DEK under AEAD, KEK in managed KMS never repo/env/browser, AAD-bound to {tenant_id,connector_id,secret_kind,version}); (3) server-only secret use (decrypt in a runner covered by `check-auth-safety`; client gets a safe-metadata DTO only); (4) re-authorized execution (server-side tenant+admin/steward role check every action; no blanket service-role, client `tenant_id` never trusted); (5) auditable/reversible/killable (append-only audit via `reject_audit_mutation`, versioned+revocable secrets, per-tenant/per-connector/global kill-switch). Full malicious-Tenant-A threat model (cross-tenant cred read, cross-tenant sync, token exfil, browser/log/error leak, service-role exploit, refresh replay, callback abuse, viewer→admin elevation, stale creds, audit poisoning) mapped to controls. **Schema + RLS are conceptual only — no migration/table/policy/encryption code; carries the `0016`/T37 privilege-surface lesson (REVOKE broad + GRANT narrow + assert `has_table_privilege`).** **No connector credentials are stored by this PR. No connector sync is implemented by this PR. No production data was touched. No hosted commands were run. No migrations were added. No RLS policies were changed. No service-role access was added. Connectors / AI / imports / exports / billing remain not built; RISK-001 stays OPEN; cutover BLOCKED; old-app replacement not yet verified.** Next: this design needs human review + acceptance before any connector implementation; the alternative fork (Option B identity matching workflow RLS/write design) remains available. **PR #100 ACCEPTED the connector vault design as the design baseline + defined the gated implementation sequence** (doc 42 §19–§23; [41 §34](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **Connector credential vault design is accepted as the design baseline. The vault is not implemented. Connectors remain not built. No connector credentials are stored. No connector sync is implemented.** **Gated sequence (doc 42 §20 — none of these PRs exist yet):** PR A vault schema migration (no execution path) → PR B RLS + deny-all secret tests → PR C server-only access wrapper + no-browser-import guard → PR D audit/run lifecycle model → PR E connector metadata UI only → PR F OAuth callback skeleton with state/nonce validation (no provider token storage until tested) → PR G first low-risk connector only after vault tests + audit pass. **Hard gates (§21):** no connector creds before vault schema + deny-all tests; no OAuth token storage before encryption-wrapper tests; no connector sync before the run/audit model; no browser exposure of secrets ever; no production credential migration before staging verification. **Acceptance does NOT mean** implementation/production approval, RISK-001 closure, cutover approval, or permission to sync; open questions (KMS provider, envelope library, local dev secret handling, rotation/revocation UX, provider OAuth callback routing, audit retention, rate-limit store) resolve per gated PR. **Connector implementation remains blocked until the gated vault implementation PRs are complete. No production data was touched. No hosted commands were run. No migrations were added. No RLS policies were changed. No service-role access was added. RISK-001 stays OPEN; cutover BLOCKED; old-app replacement not yet verified.** The next real step is **PR A (vault schema migration, no execution path)** — the first gated implementation PR — when a human starts vault build; until then connectors stay blocked. **PR #101 LANDED PR A — the CONNECTOR VAULT SCHEMA FOUNDATION** (migration `0017`; doc 42 §24; [41 §35](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **Connector vault schema foundation is added.** Migration `0017_connector_vault_schema_foundation.sql` creates the docs/42 §4 tables — `public.connectors` (Tier-1 metadata) + `public.connector_runs` (Tier-1 safe run summaries) + `public.connector_secrets` (Tier-2 secret material); audit reuses the append-only `audit_logs` (no new table). **Connector vault is still not usable. Connector secret material is not readable by authenticated users** — `connector_secrets` is RLS-enabled with ZERO policies (deny-all) + `authenticated`/`anon` hold ZERO privilege; Tier-1 metadata is tenant-member READ-only (no request-path write — a later gated PR). Proven by `org_rls_test.sql` **T38** (tenant-scoped metadata read + no-write) + **T39** (secret deny-all at runtime + `has_table_privilege` + exact-zero-privileges invariant + no-secret-column structural check); suite **248 → 292**; `test-rls.sh` re-asserts the secret-table revoke after its blanket grant (the `0015`/`0016` masking lesson); same-tenant integrity via composite `(connector_id, tenant_id)` FKs; `database.types.ts` regenerated. This PR ships §20 PR A (schema) + PR B (deny-all tests) together (a security migration is not merged untested). **Connector implementation remains blocked. No connector credentials are stored. No connector sync is implemented. No encryption/decryption wrapper is implemented. No provider connector is implemented. No OAuth callback is implemented. No connector UI is implemented. No service-role request path is added. No production data was touched. No hosted commands were run.** Next gate is **PR C** (server-only encrypt/decrypt wrapper + no-browser-import guard) — no secret of any kind is stored until its tests pass. A human must apply `0017` to staging then production in a future step (an agent never runs hosted commands). **RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #102 HARDENED the connector vault metadata/run GRANTS** (migration `0018`; doc 42 §25; [41 §36](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **Staging verification of 0017 found broad metadata/run table grants that must be hardened before the connector sequence continues:** a human applied `0017` to staging (`ycdpzduxugdsffjqyoai`) and found `connectors` + `connector_runs` holding broad `anon`/`authenticated` `INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER/SELECT` (root cause: `0017` did `grant select` but never `revoke`d the hosted-default grants — the `0015`/`0016` masking lesson; the harness re-assert had only revoked per-DML, masking it). **Connector secret material remained inaccessible to anon and authenticated users** (the bug was Tier-1 only). **Connector metadata/run grants are being hardened to least privilege:** migration `0018_harden_connector_vault_grants.sql` does `revoke all` from anon+authenticated on all three vault tables, then `grant select` to authenticated on `connectors`+`connector_runs` only — after it authenticated = `connectors:SELECT`/`connector_runs:SELECT`/`connector_secrets:(none)`, anon = (none) everywhere; no INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER for any request-path role. No write policy; `service_role` untouched; idempotent. Proven by `org_rls_test.sql` **T40** (exact per-role privilege arrays + TRUNCATE/REFERENCES/TRIGGER negatives + tenant-scoped SELECT still works + cross-tenant SELECT still RLS-denied); harness re-assert now mirrors `0018`; suite **292 → 318**; types 0-diff (grant-only). **Future-build lesson reinforced: a new `public` table migration must `REVOKE` the hosted-default grants then `GRANT` narrow, and the harness re-assert + an EXACT per-role privilege-array test (incl. REFERENCES/TRIGGER) must cover every new table.** **Connector vault is still not usable. Connector implementation remains blocked. No connector credentials are stored. No connector sync is implemented. No encryption/decryption wrapper is implemented. No provider connector is implemented. No OAuth callback is implemented. No connector UI is implemented. No service-role request path is added. No production data was touched. No hosted commands were run by the agent** (a human re-applies `0018` to staging then production; next gate is still PR C). **RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #103 RECORDED the `0018` GRANT-HARDENING STAGING VERIFICATION — PASSED** (doc 42 §26; [41 §37](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **Connector vault grant hardening has been applied and verified on staging:** a human applied `0018` to staging (`ycdpzduxugdsffjqyoai`) via `db push` and queried the live surface. **Migration 0018 is present on staging.** The table-privilege query returned **exactly two rows** — `authenticated | connector_runs | SELECT` and `authenticated | connectors | SELECT` — with **no anon rows, no connector_secrets rows, and no INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER grant for anon or authenticated**; `pg_policies` = exactly the two tenant-member SELECT policies (connectors/connector_runs), connector_secrets had no policies; linked ref remained `ycdpzduxugdsffjqyoai`. **Connector metadata tables expose authenticated SELECT only. Connector secret material remains inaccessible to anon and authenticated users. Anon has no connector vault table privileges. No broad INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, or TRIGGER grants remain on connector vault tables for anon or authenticated** — resolving the §36 finding live. **The agent ran nothing — no hosted command, no staging mutation; production untouched. Connector vault is still not usable. Connector implementation remains blocked. No connector credentials are stored. No connector sync is implemented. No encryption/decryption wrapper is implemented. No provider connector is implemented. No OAuth callback is implemented. No connector UI is implemented. No service-role request path is added. No production data was touched** (a human re-applies `0018` to production later; next gate is still PR C). **RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #104 LANDED PR C — the SERVER-ONLY CONNECTOR VAULT CRYPTO WRAPPER** (doc 42 §27; [41 §38](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **Server-only connector vault crypto wrapper is implemented and tested.** `src/lib/server/connector-vault/crypto.ts` — `encryptConnectorSecret`/`decryptConnectorSecret` over an injected `ConnectorVaultKeyProvider` (KMS abstraction). Pure AEAD: AES-256-GCM, per-secret DEK wrapped by the provider's KEK, structured payload (`v/alg/kekId/wrappedDek/iv/ciphertext/tag/aadDigest`), **AAD binds `{tenant_id, connector_id, secret_kind, version}`** (decrypt fails closed on any swap), plaintext only from `decryptConnectorSecret`, DEK zeroed, typed errors with no plaintext/key bytes. **No database access, no Supabase client import, no service-role, no `process.env`** (a test asserts the only import is `node:crypto`). Server-only boundary: under `src/lib/server/` + runtime browser-sentinel + a static guard test (no `"use client"`/`src/app` file imports it). **The wrapper uses test-only key material in tests only** (in-memory provider in the test file — random KEKs, no checked-in keys, no env secrets); no real KMS integrated. +19 tests (117 → 136): round-trip, ciphertext≠/contains-no plaintext, tenant/connector/kind/version-swap fail, tampered ciphertext/tag fail, wrong KEK fails, redacted errors, input validation, all five secret kinds, purity + server-only guards. No migration; RLS suite unchanged (318); types 0-diff. **No real connector credentials are stored. No connector secret material is inserted, updated, or deleted. No connector sync is implemented. No provider connector is implemented. No OAuth callback is implemented. No connector UI is implemented. No service-role request path is added. No production data was touched. No hosted commands were run. Connector vault is still not usable for real credentials until the remaining gated PRs are complete** (next: PR D audit/run model → PR E metadata UI → PR F OAuth callback → PR G first connector; open question §17 — the real KMS provider + local-dev secret handling — is still to be chosen). **Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #105 LANDED PR D — the CONNECTOR RUN/AUDIT LIFECYCLE FOUNDATION** (migration `0019` + server-only model; doc 42 §28; [41 §39](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **Connector run/audit lifecycle foundation is added. No connector execution is implemented. No provider connector is implemented.** Migration `0019_connector_run_audit_lifecycle.sql` widens `connector_runs` (`0017`) to the **six-state** lifecycle (`queued/running/succeeded/failed/canceled/timed_out`; `success→succeeded` is a free rewrite — zero rows), renames `finished_at→completed_at` / `items_seen→records_seen` / `error_class→failure_code`, and adds safe `records_imported` / `records_failed` / `failure_label` (no secret/token/key/payload). **Grants UNCHANGED — `authenticated` `[SELECT]` only, `anon` none, NO write policy** (run writes remain future server-only/runner work); audit reuses the **append-only `audit_logs`** (no new connector audit table); **no `connector_secrets` change.** Server-only model `src/lib/server/connector-vault/run-lifecycle.ts` (PURE — NO imports, no DB/Supabase/service-role/`process.env`): typed states + valid transitions, the conceptual audit actions (connector.run.created/.started/.completed/.failed, connector.credential.created/.revoked), pure builders that validate + return the safe shape a future runner would persist (no DB write), and a redaction guard rejecting any secret-shaped field name / credential-shaped value / unsafe failure label; the `no-client-import.test.ts` guard now covers it. +16 app tests (136 → 151) + **T41** (RLS suite **318 → 327**); types regenerated. **No connector credentials are stored. No connector secret material is inserted, updated, or deleted. No connector sync is implemented. No OAuth callback is implemented. No connector UI is implemented. No service-role request path is added. No production data was touched. No hosted commands were run. Connector vault is still not usable for real credentials until the remaining gated PRs are complete** (a human applies `0019` to staging then production later; next gate is **PR E** read-only connector metadata UI). **Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #106 fixed a post-merge lint failure** (test-only one-liner — a description on the `@ts-expect-error` for the `refresh_token` rejection case in `run-lifecycle.test.ts`; no behavior change). **PR #107 RECORDED the `0019` STAGING VERIFICATION — PASSED** (doc 42 §29; [41 §40](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **Connector run/audit lifecycle migration 0019 has been applied and verified on staging:** a human applied `0019` to staging (`ycdpzduxugdsffjqyoai`) — list showed `0019` absent before push, `supabase db push --linked` applied it, list showed `0019` present; linked ref remained `ycdpzduxugdsffjqyoai`. **Connector_runs supports queued, running, succeeded, failed, canceled, and timed_out** (live `connector_runs_status_check`). The table-privilege query returned **exactly two rows** (`authenticated | connector_runs | SELECT` and `authenticated | connectors | SELECT`) — no anon rows, no connector_secrets rows; `pg_policies` = exactly the two tenant-member SELECT policies and **connector_secrets has no policies.** **Connector metadata tables expose authenticated SELECT only. Anon has no connector vault table privileges. Connector secret material remains inaccessible to anon and authenticated users. No broad INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, or TRIGGER grants remain on connector vault tables for anon or authenticated.** **The agent ran nothing — no hosted command, no staging mutation; production untouched. Connector vault is still not usable. Connector implementation remains blocked. No connector credentials are stored. No connector sync is implemented. No provider connector is implemented. No OAuth callback is implemented. No connector UI is implemented. No service-role request path is added. No production data was touched** (a human re-applies `0019` to production later; next gate is still **PR E**). **RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #108 LANDED PR E — the READ-ONLY CONNECTOR METADATA UI** (doc 42 §30; [41 §41](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **Read-only connector metadata UI is added. Only safe connector and connector run metadata is shown. Connector secret material is not queried or displayed.** A new authenticated route `/connectors` (route count **16 → 17**) + the nav "Connectors" item flipped from "Not built yet" to `/connectors`; mirrors the read-only `/files`/`/reports`/`/admin` pattern (user-scoped client, RLS is the authority, fail-closed). `src/lib/data/connectors.ts` `listConnectorsForCurrentUser()` does two RLS-scoped reads of the **Tier-1 tables only** — `connectors` (provider/display_name/status/granted_scopes_safe/timestamps) + `connector_runs` (latest run per connector: status/timestamps/safe failure_code+label/safe counters) — and **never queries `connector_secrets`** nor selects `tenant_id`/`organization_id`/`connected_by`/`health`/`last_sync_at`. The page shows the safe table + empty state + an explicit "Not built yet" list (connect a provider / store credentials / OAuth callback / API key·PAT entry / run sync / provider connectors / disconnect·revoke / manual run / scheduled run / real connector health). +7 app tests (151 → 158; build 16 → 17 routes; RLS suite unchanged **327**, no migration, types 0-diff), incl. a static scan proving no `connector_secrets` query / secret-shaped column string in the page+data code. **No connector credentials are stored. No connector sync is implemented. No provider connector is implemented. No OAuth callback is implemented. No credential form is implemented. No connect/reconnect/disconnect action is implemented. No manual or scheduled run action is implemented. No service-role request path is added. No production data was touched. No hosted commands were run. Connector vault is still not usable for real credentials until the remaining gated PRs are complete** (next: PR F OAuth callback skeleton → PR G first connector). **Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #109 LANDED PR F — the OAUTH CALLBACK VALIDATION SKELETON** (doc 42 §31; [41 §42](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **OAuth callback skeleton is added. OAuth state/nonce validation is implemented.** `src/lib/server/connector-vault/oauth-state.ts` (server-only, PURE — only import `node:crypto`): a stateless **HMAC-SHA256-signed** `state` binds `{tenant_id, provider, connector_id?, subject?, redirect_intent, nonce, exp}`; `validateOAuthState` verifies the HMAC over the exact signed bytes **before** trusting any field (constant-time), then nonce/expiry/optional tenant-provider-connector binding/optional single-use replay, returning a **safe reason CODE only** (never a secret/nonce/token/code). The signing key is an **injected signer** (server-only secret/KMS in prod — NOT here; test-only in-memory signer in tests). Single-use replay via an injected `ConsumedNonceStore` (in-memory in tests); the **production DB-backed `oauth_pending` replay store remains a gate** (no DB write here). An inert callback route `/connectors/oauth/callback` (route count **17 → 18**) parses provider/code/state/error, builds the signer from a server-only env secret **this PR does not set** (inert "not configured" by default), and returns a **safe plain-text inert response** — **no OAuth code is exchanged** (the `code` value is never read/returned/logged), never calls a provider endpoint, never writes `connector_secrets`, never marks a connector connected, never persists query params. +26 app tests (158 → 184; build 17 → 18 routes; RLS suite unchanged **327**, no migration, types 0-diff): tampered/wrong-tenant/wrong-provider/wrong-connector/expired/missing-nonce/wrong-key/replay all fail with the right reason; handler rejects missing/tampered state + does not exchange the code; a static scan proving no `fetch`/`createClient`/`process.env`(module)/`connector_secrets`/`service_role`/`access_token`/`refresh_token`/`token_endpoint`/`grant_type`; no-client-import guard covers oauth-state (inert route is the only allowed `src/app` importer). **No OAuth code is exchanged for tokens. No access token is stored. No refresh token is stored. No connector credentials are stored. No connector secret material is inserted, updated, or deleted. No connector sync is implemented. No provider connector is implemented. No credential form is implemented. No connect/reconnect/disconnect action is implemented. No manual or scheduled run action is implemented. No service-role request path is added. No production data was touched. No hosted commands were run. Connector vault is still not usable for real credentials until the remaining gated PRs are complete** (next: PR G first connector — only after the production signer/KMS secret + the single-use `oauth_pending` replay store are wired and tested). **Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #110 RECORDED the KMS / OAuth-signer / local-dev-secret / `oauth_pending` REPLAY-STORE DECISION** (docs-only; doc 42 §32 + §17 resolved; [41 §43](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **KMS/key-provider decision is recorded. OAuth state signer decision is recorded. OAuth replay-store design is recorded.** **(§32.1) KMS:** production `ConnectorVaultKeyProvider` backed by an **external managed KMS** (default AWS KMS / GCP KMS) holding the KEK; **Supabase Vault/pgsodium rejected** (co-locates key+ciphertext). KEK owned by the server-only runner identity (KMS `GenerateDataKey`/`Decrypt` only, never a request-path role); `kekId` = a non-sensitive KMS handle per wrapped DEK; rotation by alias (no mass re-encryption); unwrap failure **fails closed** (typed error, no key/plaintext, run `failed`); local-dev uses the in-memory test provider — **no committed keys, no prod secret read in tests**. **(§32.2) OAuth signer:** a **server-only ≥32-byte HMAC secret** from the host secret store (`CONNECTOR_OAUTH_STATE_SECRET`), never in repo/migration/client; rotation accepts {current, previous} keys for a grace window = max state TTL (≤10 min); `state` carries only the signature; failures return a safe reason code only. **(§32.3) `oauth_pending` replay store (design-only, no migration):** a Tier-2 table (`tenant_id`/provider/connector_id?/subject?/`state_jti`/`nonce_hash` [sha256, raw nonce never stored]/`expires_at`/`consumed_at?`/`created_at`/safe attempt+reason metadata) with `UNIQUE(state_jti)`/`UNIQUE(nonce_hash)`; **single-use consume = one atomic UPDATE** (server-only); **RLS deny-all + zero anon/authenticated privilege** (mirrors `connector_secrets`); constant-time hash compare; scheduled expiry sweep; `connector.oauth.state.created/.consumed/.expired/.rejected` → append-only `audit_logs` (safe metadata only). **(§32.4) Gates:** no real OAuth token storage before the replay store is implemented+tested; no real credential storage before the production KMS provider is implemented+tested; no provider connector before replay store + key provider + audit path complete; no browser credential form until the secret write path is explicitly reviewed; no production credential storage before staging verification. **No real connector credential storage is implemented. No OAuth token exchange is implemented. No access token is stored. No refresh token is stored. No connector secret material is inserted, updated, or deleted. No connector sync is implemented. No provider connector is implemented. No credential form is implemented. No connect/reconnect/disconnect action is implemented. No manual or scheduled run action is implemented. No service-role request path is added. No production data was touched. No hosted commands were run. Connector vault is still not usable for real credentials until the remaining gated PRs are complete** (next: PR G first connector — only after §32.1 KMS provider + §32.3 `oauth_pending` replay store are implemented and tested). **Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #111 LANDED the `oauth_pending` SINGLE-USE REPLAY STORE** (migration `0020` + pure server-only helper; doc 42 §33; [41 §44](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **OAuth pending replay store schema is added. The oauth_pending table is not readable or writable by anon or authenticated users. OAuth replay-store implementation remains server-only.** Migration `0020_oauth_pending_replay_store.sql` creates `public.oauth_pending` (`tenant_id`/`organization_id?`/`connector_id?`/provider/`subject?`/`state_jti`/`nonce_hash` [sha256 — **raw nonce never stored**]/`intent`/`expires_at` NOT NULL/`consumed_at?`/`created_at`/`attempt_count`/`last_rejected_code?`) — **no raw nonce/state/code/token/secret/PKCE column**; single-use `UNIQUE(state_jti)`+`UNIQUE(nonce_hash)`; same-tenant composite `(connector_id, tenant_id)` FK (MATCH SIMPLE) blocks cross-tenant binding. **RLS deny-all (mirrors `connector_secrets`):** RLS-enabled + ZERO policies + `revoke all` from anon+authenticated (no grant, no read/write policy) — after `0020` authenticated+anon hold EXACTLY zero privilege; the future server-only consume path (runner / `SECURITY DEFINER`, a later PR) does the atomic single-use UPDATE; `test-rls.sh` re-asserts the `oauth_pending` revoke after its blanket-grant crutch. Pure server-only helper `src/lib/server/connector-vault/oauth-pending.ts` (only `node:crypto`): `hashOAuthValue` (deterministic sha256) + `buildOAuthPendingRecord` (validates + returns the safe row, hashing the raw nonce and never returning it, rejecting secret-shaped fields); no DB/token-exchange/`connector_secrets`/service-role; server-only (sentinel + no-client-import guard). **T42** (RLS suite **327 → 352**): deny-all (runtime + catalog exact-zero-privilege) + structural (RLS, zero policies, no secret column, `expires_at` NOT NULL, UNIQUE single-use rejects duplicates, composite-FK cross-tenant block) + `connector_secrets`/Tier-1 grants unchanged; **+9 app tests** (184 → 193); types regenerated. **No OAuth code is exchanged for tokens. No access token is stored. No refresh token is stored. No connector credentials are stored. No connector secret material is inserted, updated, or deleted. No connector sync is implemented. No provider connector is implemented. No credential form is implemented. No connect/reconnect/disconnect action is implemented. No manual or scheduled run action is implemented. No service-role request path is added. No production data was touched. No hosted commands were run. Connector vault is still not usable for real credentials until the remaining gated PRs are complete** (next: §32.1 KMS provider → server-only consume path → PR G; a human applies `0020` to staging then production later). **Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #112 RECORDED the `0020` STAGING VERIFICATION — PASSED** (doc 42 §34; [41 §45](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **OAuth pending replay store migration 0020 has been applied and verified on staging:** a human applied `0020` to staging (`ycdpzduxugdsffjqyoai`) — list showed `0020` absent before push, `supabase db push --linked` applied it, list showed `0020` present; linked ref remained `ycdpzduxugdsffjqyoai`. The live RLS query returned `connector_secrets rls_enabled = true` and `oauth_pending rls_enabled = true` — **Oauth_pending RLS is enabled.** The table-privilege query returned **exactly two rows** (`authenticated | connector_runs | SELECT` and `authenticated | connectors | SELECT`) with no anon rows, no connector_secrets rows, no oauth_pending rows; `pg_policies` = exactly the two tenant-member SELECT policies. **Oauth_pending is not readable or writable by anon or authenticated users. Oauth_pending has no policies. Connector secret material remains inaccessible to anon and authenticated users. Connector_secrets has no policies. Connector metadata tables expose authenticated SELECT only. Anon has no connector vault table privileges. No broad INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, or TRIGGER grants remain on connector vault tables for anon or authenticated.** **The agent ran nothing — no hosted command, no staging mutation; production untouched. No OAuth code is exchanged for tokens. No access token is stored. No refresh token is stored. No connector credentials are stored. No connector secret material is inserted, updated, or deleted. No connector sync is implemented. No provider connector is implemented. No credential form is implemented. No connect/reconnect/disconnect action is implemented. No manual or scheduled run action is implemented. No service-role request path is added. No production data was touched** (a human re-applies `0020` to production later; next is §32.1 KMS provider → server-only consume path → PR G). **Connector vault is still not usable for real credentials until the remaining gated PRs are complete. Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #113 LANDED the KMS-BACKED `ConnectorVaultKeyProvider` SKELETON** (server-only adapter; doc 42 §35; [41 §46](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **KMS-backed ConnectorVaultKeyProvider skeleton is added. The KMS adapter is server-only. Tests use mocked or test-only key material only; no real KMS credentials are required in tests.** `src/lib/server/connector-vault/kms-key-provider.ts` adapts the §32.1 external-KMS decision to the PR C `ConnectorVaultKeyProvider` interface via a **dependency-free `KmsClient` boundary** (`generateDataKey`/`decrypt`, mapping 1:1 to AWS KMS `GenerateDataKey`/`Decrypt`; GCP equivalent) — **NO AWS/GCP SDK added** (a real KMS-backed client is a later PR, mocked in tests; a test-only in-memory fake stands in). `createKmsKeyProvider(config)` wraps new secrets **only under the current KEK**, unwraps under current OR previous (rotation grace — rotate by alias, no re-encryption), rejects an unknown key id before any KMS call, **fails closed** when unconfigured (`kmsKeyProviderConfigFromEnv` returns null when `CONNECTOR_VAULT_KMS_KEY_ID` unset — this PR sets nothing + binds no real client, so production stays inert), and throws **redacted** typed `ConnectorVaultKeyProviderError`s (no plaintext/key/wrapped/ciphertext bytes; the KmsClient error is swallowed; nothing logs). Exposes non-secret `currentKekId`/`allowedKekIds` metadata. Server-only (sentinel + no-client-import guard; only the erased `./crypto` type import). +11 app tests (193 → 204; RLS unchanged **352**, no migration, types 0-diff): wrap/unwrap, rotation, wrong/unknown key id, missing-config fail-closed, redacted wrap/unwrap-failure errors, invalid-DEK fail-closed, env-helper null-when-unset, **the crypto wrapper round-trips THROUGH the provider** (no real KMS), module purity. **No OAuth code is exchanged for tokens. No access token is stored. No refresh token is stored. No connector credentials are stored. No connector secret material is inserted, updated, deleted, or read. No connector sync is implemented. No provider connector is implemented. No credential form is implemented. No connect/reconnect/disconnect action is implemented. No manual or scheduled run action is implemented. No service-role request path is added. No production data was touched. No hosted commands were run. Connector vault is still not usable for real credentials until the remaining gated PRs are complete** (next: a reviewed real KMS-backed `KmsClient` + the server-only `oauth_pending` consume path → PR G). **Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #114 LANDED the REAL AWS KMS CLIENT ADAPTER SKELETON** (server-only adapter; doc 42 §36; [41 §47](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **Real KMS client adapter skeleton is added. The adapter is server-only. Tests use mocked KMS responses only; no real KMS credentials are required in tests; no live KMS calls are made in tests.** `src/lib/server/connector-vault/aws-kms-client.ts` is the concrete `KmsClient` (PR #113 boundary) for the §32.1 chosen provider, **AWS KMS**. It emits the exact AWS KMS command shapes — wrap → `GenerateDataKey { KeyId, KeySpec: "AES_256" }` → `{ Plaintext (DEK), CiphertextBlob }`; unwrap → `Decrypt { KeyId, CiphertextBlob }` → `{ Plaintext }` (passing `KeyId` makes KMS enforce the blob was wrapped under that key) — through an **injected `AwsKmsCommandSender`**. **NO `@aws-sdk/client-kms` dependency is added**; wiring a real SDK-backed sender (`new KMSClient({region}).send(...)`) is the **next gate** (the one PR where the SDK is introduced, mocked in its tests), so this adapter's tests need no AWS credentials + make no live call. `createAwsKmsClient({ send, region })` **fails closed** on missing `send` / garbage `region` (AWS region format checked); `awsKmsConfigFromEnv()` returns null when `CONNECTOR_VAULT_AWS_KMS_REGION` unset/invalid + binds no sender (inert by default); throws **redacted** typed `AwsKmsError`s (no plaintext/key/blob/region/SDK detail; sender error swallowed; a missing/short `Plaintext` fails closed; nothing logs). Server-only (sentinel + no-client-import guard; only the erased `./kms-key-provider` type import); wired to nothing — reached only via `createKmsKeyProvider` later. +11 app tests (204 → 215; RLS unchanged **352**, no migration, types 0-diff): missing-config fail-closed, wrap/unwrap command-shape mapping, mocked-success-via-contract, redacted KMS error, malformed-response fail-closed, wrong-KeyId-on-Decrypt fail-closed, env null-when-unset, **the AWS adapter composes through `createKmsKeyProvider` + the crypto wrapper round-trip** (no real KMS), module purity. **No OAuth code is exchanged for tokens. No access token is stored. No refresh token is stored. No connector credentials are stored. No connector secret material is inserted, updated, deleted, or read. No connector sync is implemented. No provider connector is implemented. No credential form is implemented. No connect/reconnect/disconnect action is implemented. No manual or scheduled run action is implemented. No service-role request path is added. No production data was touched. No hosted commands were run. Connector vault is still not usable for real credentials until the remaining gated PRs are complete** (next: the SDK-wiring gate + the server-only `oauth_pending` consume path → PR G). **Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #115 LANDED the AWS KMS SDK SENDER WIRING** (server-only; doc 42 §37; [41 §48](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **AWS KMS SDK sender wiring is added. The AWS KMS sender is server-only. Tests mock AWS KMS responses only; no real AWS or KMS credentials are required in tests; no live KMS calls are made in tests.** `src/lib/server/connector-vault/aws-kms-sdk-sender.ts` is the concrete `@aws-sdk/client-kms`-backed implementation of the `AwsKmsCommandSender` seam (PR #114). **This PR adds the `@aws-sdk/client-kms` dependency** — the single dependency the §32.1/§36 plan reserved for this gate, the only place it is imported (the 2 moderate `npm audit` advisories are PRE-EXISTING `next`→`postcss` transitive, NOT from the SDK; `audit fix --force` not run). The SDK import is server-only (sentinel + no-client-import guard + `next build` confirm no client/route reaches it → not bundled into a browser route). `awsKmsSenderFromClient(client)` builds the real `GenerateDataKeyCommand { KeyId, KeySpec: "AES_256" }` / `DecryptCommand { KeyId, CiphertextBlob }`, calls `client.send(command)`, maps the SDK output to `AwsKmsResponse` — tests inject a MOCK `{ send }` (no SDK construction, no network, no creds). `createAwsKmsSdkSender({ region })` validates region (fails closed before any client) + constructs `new KMSClient({ region })` (credentials via the runner IAM default provider chain — never hardcoded); `createAwsKmsSdkSenderFromEnv()` returns null unless `CONNECTOR_VAULT_AWS_KMS_REGION` set (this PR sets no env — inert). Redaction: send-failure/malformed → typed `AwsKmsSdkError` (raw AWS error swallowed; nothing logs); the §36 adapter re-validates. +10 app tests (215 → 225; RLS unchanged **352**, no migration, types 0-diff): fail-closed config/no client; wrap/unwrap SDK command-shape mapping (real Command instances via a mock client — no live call); raw AWS error swallowed; malformed/missing-Plaintext fails closed; env null-when-unset; **the mocked SDK sender composes through `createAwsKmsClient` + `createKmsKeyProvider` + the crypto wrapper round-trip** (no real KMS); module scope. **No OAuth code is exchanged for tokens. No access token is stored. No refresh token is stored. No connector credentials are stored. No connector secret material is inserted, updated, deleted, or read. No connector sync is implemented. No provider connector is implemented. No credential form is implemented. No connect/reconnect/disconnect action is implemented. No manual or scheduled run action is implemented. No service-role request path is added. No production data was touched. No hosted commands were run. No environment variable is added to production or staging. Connector vault is still not usable for real credentials until the remaining gated PRs are complete** (next: the server-only `oauth_pending` consume path → PR G — only after the runner IAM/KMS grant + a real KEK alias are provisioned + staging-verified by a human). **Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #116 LANDED the SERVER-ONLY `oauth_pending` CONSUME PATH** (doc 42 §38; [41 §49](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **Server-only oauth_pending consume path is added. The consume path performs atomic single-use consumption. Oauth_pending remains not directly readable or writable by anon or authenticated users.** `src/lib/server/connector-vault/oauth-pending-consume.ts` (PURE — NO imports, server-only): `oauth_pending` is Tier-2 deny-all (`0020`, T42), so a request-path client cannot touch it — the module ships the consume LOGIC + classification and delegates the privileged write to an **injected `OAuthPendingConsumer`** (the runner-identity-backed executor, backed by a `SECURITY DEFINER` accessor / the runner's connection, never request/browser-reachable, is a later gated PR). **No browser-accessible service-role path is added; no migration; RLS stays 352.** Atomic single-use: the executor's `runAtomicConsume` runs ONE statement (`update … set consumed_at=$now where state_jti/nonce_hash/tenant_id/provider match, connector_id is not distinct from $connector_id, consumed_at is null, expires_at > $now returning …`); success = exactly one row changed; on 0 rows `consumeOAuthPending` does a READ-ONLY classify (by the unique `state_jti`) → a safe reason code (not_found/already_consumed/expired/tenant/provider/connector/nonce mismatch/malformed_input), never mutating again. Result is a safe reason CODE + non-secret metadata (`stateJti`, `consumedAt`) — never a raw nonce/state/code/secret. +16 app tests (225 → 241; RLS unchanged **352**, no migration, types 0-diff): consume-exactly-one (+ one atomic mutation); second-consume `already_consumed`; fresh-connect (null) consume; every failure → its safe reason; malformed never reaches the mutation; missing-consumer throws typed; failure echoes no raw value; module purity; **the OAuth callback route still exchanges no code + stores no token**; T42/T39/T40 deny-all proofs unchanged. **No OAuth code is exchanged for tokens. No access token is stored. No refresh token is stored. No connector credentials are stored. No connector secret material is inserted, updated, deleted, or read. No connector sync is implemented. No provider connector is implemented. No credential form is implemented. No connect/reconnect/disconnect action is implemented. No manual or scheduled run action is implemented. No browser-accessible service-role path is added. No production data was touched. No hosted commands were run. Connector vault is still not usable for real credentials until the remaining gated PRs are complete** (next: PR G — a first low-risk connector — only after the runner-identity-backed executor + the IAM/KMS grant + a real KEK alias are provisioned + staging-verified by a human). **Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #117 RECORDED the CONNECTOR RUNNER PROVISIONING PLAN** (docs-only; doc 42 §39; [41 §50](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **Runner identity provisioning plan is recorded. KMS/IAM provisioning plan is recorded. OAuth consume execution plan is recorded. No runner identity is implemented by this PR. No runner DB grants are added by this PR. No real KMS/IAM grant is configured by this PR.** Records what the built code primitives (PR #101–#116) require before any real credential storage / provider connector: **(§39.1) Runner identity** — a single dedicated server-side connector-runner principal (a worker/job, NOT a request handler) executes the privileged vault actions, reached ONLY from server-only `connector-vault/*` modules via the runner entrypoint, NEVER a browser/route/server-action; a narrow dedicated identity (own DB role + narrow AWS IAM), NOT the broad `service_role` on a request path; the user request path stays RLS-deny-all on `oauth_pending`/`connector_secrets`. **(§39.2) DB privileges (least privilege, none added here)** — runner = `oauth_pending` SELECT+INSERT+UPDATE(consumed_at)+expiry-sweep; `connector_secrets` INSERT+SELECT+UPDATE(is_active/revoked), no DELETE; `connectors`/`connector_runs` narrow metadata write (the `authenticated` `[SELECT]`-only surface unchanged); no anon, no normal-authenticated direct access, no TRUNCATE/REFERENCES/TRIGGER; every privileged action audits to append-only `audit_logs` (safe metadata only). **(§39.3) OAuth consume** — runner-only atomic single-use consume (PR #116); expired/reused/mismatched fail closed; raw nonce/state/code never persisted/logged; no token exchange until consume is implemented + staging-verified. **(§39.4) KMS/IAM** — per-env KEK alias (`alias/idcaddie-connector-vault-kek-{staging,prod}`); region from a server-only env var; runner IAM granted ONLY `kms:GenerateDataKey`+`kms:Decrypt` scoped to the single KEK (no `kms:*`, no `Resource: *`); rotation by alias; staging/prod separation; no committed credentials (prefer an IAM role → no AWS keys in app env). **(§39.5) Env** — server-only conceptual vars, never in a client bundle, fail-closed when missing; this PR sets none. **(§39.6) Staging verification (human-executed)** — apply the future runner-grant migration to staging first; verify `oauth_pending`/`connector_secrets` still inaccessible to anon/authenticated; verify the runner holds EXACTLY the §39.2 privileges; verify no browser path invokes runner ops; verify the KMS path with a staging-safe/non-production key; record evidence before connector work. **(§39.7) Gates** — no real credential storage before runner DB grants + KMS IAM are implemented + staging-verified; no provider connector before runner consume + KMS path implemented + staging-verified; no production credential flow before the production migration + IAM/KMS applied + verified; RISK-001 stays OPEN until the full flow is built/verified + doc 17 §5 met. RLS unchanged **352**, no migration, no env secret. **No OAuth code is exchanged for tokens. No access token is stored. No refresh token is stored. No connector credentials are stored. No connector secret material is inserted, updated, deleted, or read. No connector sync is implemented. No provider connector is implemented. No credential form is implemented. No connect/reconnect/disconnect action is implemented. No manual or scheduled run action is implemented. No browser-accessible service-role path is added. No production data was touched. No hosted commands were run. Connector vault is still not usable for real credentials until the remaining gated PRs are complete. Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #118 LANDED the CONNECTOR_RUNNER DB GRANT FOUNDATION** (migration `0021`; doc 42 §40; [41 §51](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **Runner DB grant foundation is added. Runner privileges are least-privilege and not granted to anon or authenticated users. Oauth_pending remains not directly readable or writable by anon or authenticated users. Connector secret material remains inaccessible to anon and authenticated users. No browser-accessible service-role path is added.** Migration `0021_connector_runner_grants.sql` creates a dedicated server-side `connector_runner` DB principal (`create role connector_runner nologin bypassrls`, idempotent) granted the minimum the OAuth `oauth_pending` consume (PR #116) needs, and nothing else; no app request path is wired to the runner (no `src/`/`src/app` reference). **BYPASSRLS justified + constrained:** `oauth_pending` is RLS deny-all (zero policies), so a plain grant alone is RLS-denied; the runner is the §3 trusted server principal whose tenant-bound query contract (PR #116's `update … where tenant_id=$tid and state_jti=$jti and nonce_hash=$nh and consumed_at is null and expires_at > now()`) excludes cross-tenant rows in the WHERE, not via RLS; it is NOT the broad `service_role` on a request path. **Grants (least privilege, `oauth_pending` only):** SELECT + a column-level UPDATE on EXACTLY {consumed_at, attempt_count, last_rejected_code}; no INSERT, no row delete/purge, no REFERENCES, no TRIGGER, no UPDATE on the immutable identity columns. **DEFERRED (no grant here):** `connector_secrets` (secret read/write is a later PR — tombstone/version) and `connectors`/`connector_runs` (lifecycle write is a later PR); the secret-table deny-all to anon/authenticated is re-asserted; no policy is added for any browser role. **T43** (RLS suite **352 → 387**, grant-only — types 0-diff, no app change): runner exists, BYPASSRLS+NOLOGIN, EXACTLY SELECT + the 3-column UPDATE (no INSERT/DELETE/TRUNCATE/REFERENCES/TRIGGER, no identity-column UPDATE), ZERO on connector_secrets/connectors/connector_runs; functionally can consume (SELECT + set consumed_at) but not delete/insert/update-identity/read-secrets; anon/authenticated deny-all unchanged (a normal authenticated user still cannot consume oauth_pending or touch connector_secrets); the harness applies `0021` so the runner privileges reflect EXACTLY `0021`. A human applies `0021` to staging then production later + records the §39.6 verification before connector work. **No OAuth code is exchanged for tokens. No access token is stored. No refresh token is stored. No connector credentials are stored. No connector secret material is inserted, updated, deleted, or read by app code. No connector sync is implemented. No provider connector is implemented. No credential form is implemented. No connect/reconnect/disconnect action is implemented. No manual or scheduled run action is implemented. No production data was touched. No hosted commands were run. Connector vault is still not usable for real credentials until the remaining gated PRs are complete. Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #119 RECORDED the `0021` STAGING VERIFICATION — PASSED** (doc 42 §41; [41 §52](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **Connector runner DB grant migration 0021 has been applied and verified on staging:** a human applied `0021` to staging (`ycdpzduxugdsffjqyoai`) — list showed `0021` absent before push, `supabase db push --linked` applied it, list showed `0021` present; linked ref remained `ycdpzduxugdsffjqyoai`. The role query confirmed `connector_runner` with `rolcanlogin = false` / `rolbypassrls = true` — **Connector_runner is NOLOGIN. Connector_runner has BYPASSRLS only for the narrow runner consume path.** The table-privilege query returned **exactly three rows** (`authenticated | connector_runs | SELECT`, `authenticated | connectors | SELECT`, `connector_runner | oauth_pending | SELECT`) — no anon rows, no connector_secrets rows, no connector_runner privilege on connectors/connector_runs. **Connector_runner has SELECT on oauth_pending. Connector_runner has column-scoped UPDATE only on consumed_at, attempt_count, and last_rejected_code. Connector_runner has no connector_secrets privileges. Connector_runner has no connectors or connector_runs privileges.** `pg_policies` = exactly the two tenant-member SELECT policies. **Oauth_pending remains not directly readable or writable by anon or authenticated users. Oauth_pending has no policies. Connector secret material remains inaccessible to anon and authenticated users. Connector_secrets has no policies. Connector metadata tables expose authenticated SELECT only. Anon has no connector vault table privileges. No broad INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, or TRIGGER grants remain on connector vault tables for anon or authenticated.** **The agent ran nothing — no hosted command, no staging mutation; production untouched. No OAuth code is exchanged for tokens. No access token is stored. No refresh token is stored. No connector credentials are stored. No connector secret material is inserted, updated, deleted, or read. No connector sync is implemented. No provider connector is implemented. No credential form is implemented. No connect/reconnect/disconnect action is implemented. No manual or scheduled run action is implemented. No browser-accessible service-role request path is added. No production data was touched** (a human re-applies `0021` to production later; next are the runner-identity-backed executors + the KMS/IAM provisioning → PR G). **Connector vault is still not usable for real credentials until the remaining gated PRs are complete. Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #120 LANDED the RUNNER-ONLY `oauth_pending` EXECUTOR WIRING** (doc 42 §42; [41 §53](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **Runner-only oauth_pending executor wiring is added. The executor is server-only. The executor is not exposed to browser request paths.** `src/lib/server/connector-vault/oauth-pending-executor.ts` — the concrete `OAuthPendingConsumer` (PR #116) backed by an INJECTED `RunnerDbClient` (`run(sql, params) → { rows }`; the future runner backs it with a server-only `connector_runner` Postgres connection — `0021`, T43, staging-verified). **No global service-role/admin client is created — the client is explicitly injected; tests mock it (no live DB call, no credentials).** `createOAuthPendingExecutor(client)` **fails closed** if the client is missing; `runAtomicConsume` issues ONE parameterized statement (match tenant/provider/state_jti/nonce_hash/connector_id null-safe + `consumed_at is null` + `expires_at > now`, set ONLY `consumed_at`); `readPendingState` is the read-only classify lookup. Composed with the pure `consumeOAuthPending` → the single-use consume. **Redaction:** DB errors throw a fixed safe message — never a raw nonce/state/code/token/secret/DB body; the nonce HASH + ids are bound params; nothing logs. Server-only (sentinel + `no-client-import` guard; only the consume TYPES imported). **No app route/server action/browser path calls it** (the callback route stays inert — static scan). **Oauth_pending remains not directly readable or writable by anon or authenticated users. Connector secret material remains inaccessible to anon and authenticated users.** +12 app tests (241 → 253; RLS unchanged **387**, no migration, no dependency, types 0-diff): fail-closed on a missing client; the consume UPDATE shape + bound params; the classify SELECT; the full chain through `consumeOAuthPending` with a mock that models the atomic single-use semantics; both DB errors redacted; module purity; the OAuth callback route still inert. A future PR wires the real `RunnerDbClient` + the IAM/KMS grant → PR G. **No OAuth code is exchanged for tokens. No access token is stored. No refresh token is stored. No connector credentials are stored. No connector secret material is inserted, updated, deleted, or read. No connector sync is implemented. No provider connector is implemented. No credential form is implemented. No connect/reconnect/disconnect action is implemented. No manual or scheduled run action is implemented. No browser-accessible service-role request path is added. No production data was touched. No hosted commands were run. Connector vault is still not usable for real credentials until the remaining gated PRs are complete. Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #121 RECORDED the HOSTED RUNNER/KMS WIRING PLAN + NO-REAL-TOKEN STAGING VERIFICATION CHECKLIST** (docs-only; doc 42 §43; [41 §54](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **Hosted runner/KMS wiring plan is recorded. No hosted runner is implemented by this PR. No real KMS/IAM grant is configured by this PR.** Now that the connector-vault primitives exist (PR #101–#120) but the hosted runner/KMS path is not provisioned or verified, this records what a human must provision + verify before any real provider connector or real token storage: **(§43.1) Runner DB:** the runner (a server-only worker, NOT a request handler) opens a direct Postgres connection AS `connector_runner` (filling the PR #120 `RunnerDbClient`); its connection secret lives ONLY in the runner host's secret manager — never the repo/app env/client bundle; reached only from the runner entrypoint, never a route — NOT a browser/request service-role path; it can ONLY consume `oauth_pending` (SELECT + the 3-column UPDATE — `0021`), not INSERT/DELETE it or touch `connector_secrets`/`connectors`/`connector_runs`. **(§43.2) AWS KMS:** per-env KEK alias (`alias/idcaddie-connector-vault-kek-{staging,prod}`), staging/prod separation, the runner host's IAM role granted ONLY `kms:GenerateDataKey`+`kms:Decrypt` scoped to the single KEK (no `kms:*`, no `Resource: *`), no static AWS keys in the repo, no AWS keys in any browser/client env. **(§43.3) Env:** server-only conceptual vars, staging/prod separation, fail-closed when missing; this PR sets none. **(§43.4) No-real-token staging verification (human-executed):** runner DB can consume `oauth_pending` only; `oauth_pending`/`connector_secrets` stay deny-all to anon/authenticated; a KMS mock/staging-safe dry-run wrap/unwrap WITHOUT a provider token; no token exchange; no credential storage; no browser path invokes runner ops; no KMS SDK bundle in the browser. **(§43.5) Gates before the first connector:** runner DB wiring implemented + staging-verified; AWS KMS/IAM/KEK staging path implemented + staging-verified; the no-real-token full vault chain verified end-to-end; THEN a first low-risk connector skeleton may be started. **(§43.6) Explicit non-approval:** does NOT approve real token storage / connector sync / production; does NOT close RISK-001; does NOT unblock cutover. RLS unchanged **387**, no migration, no env secret. **No OAuth code is exchanged for tokens. No access token is stored. No refresh token is stored. No connector credentials are stored. No connector secret material is inserted, updated, deleted, or read. No connector sync is implemented. No provider connector is implemented. No credential form is implemented. No connect/reconnect/disconnect action is implemented. No manual or scheduled run action is implemented. No browser-accessible service-role request path is added. No production data was touched. No hosted commands were run. Connector vault is still not usable for real credentials until hosted runner/KMS verification is complete. Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.** **PR #122 ADDED the CONNECTOR-VAULT STAGING DRY-RUN VERIFIER** (`scripts/verify-staging-connector-vault-dry-run.mjs`; doc 42 §44; [41 §55](./41_FULL_PARITY_IMPLEMENTATION_ROADMAP.md)). **Hosted runner/KMS no-real-token verifier is added. The verifier is human-run only. The agent did not run hosted commands.** The executable form of the §43.4 checklist — a guarded gate a human runs to PROVE the hosted runner DB connection (as `connector_runner`) + the AWS KMS path work WITHOUT storing any real credential. Same safety model as `verify-staging-rls-suite.mjs`: connects to NOTHING, prints NO secret values, performs NO hosted mutation — even the confirmed path only PRINTS an ordered runbook (parameterized psql/KMS commands referencing shell env VARS by name) the operator runs + records. **Guards:** **The verifier refuses production**; requires staging (`ycdpzduxugdsffjqyoai`) via the linked file or `--ref`; requires an explicit confirmation phrase before emitting the runbook (default refuses; `--help` prints usage); requires hosted secrets/config via ENV (names only — never read/printed); **the verifier does not use real provider tokens** (only the synthetic sentinel `synthetic-vault-dry-run-not-a-token`). **Runbook proves (human-run):** consume one synthetic `oauth_pending` row exactly once as `connector_runner`; second consume + every mismatch → 0 rows; runner DENIED on `connector_secrets`; `oauth_pending`/`connector_secrets` deny-all to anon/authenticated; KMS wrap/unwrap of the synthetic payload (GenerateDataKey + Decrypt only); narrow cleanup; no browser route involved. +13 tests (253 → 266; RLS unchanged **387**, no migration, no dependency, types 0-diff): mocks only, no hosted call — refuses production/off-staging/no-confirmation/missing-env; emits the runbook only when confirmed + staging + env (still opens no connection); redacts secrets; synthetic payload only; no `connector_secrets` write printed; `--help` exits 0; source imports only `node:fs`. A human runs it on staging next + records the evidence; the agent runs only `node --check` + the mock tests. **No OAuth code is exchanged for tokens. No access token is stored. No refresh token is stored. No connector credentials are stored. No connector secret material is inserted, updated, deleted, or read. No connector sync is implemented. No provider connector is implemented. No credential form is implemented. No connect/reconnect/disconnect action is implemented. No manual or scheduled run action is implemented. No browser-accessible service-role request path is added. No production data was touched. No hosted commands were run by the agent. Connector vault is still not usable for real credentials until the human-run staging dry run is executed and recorded. Connector implementation remains blocked. RISK-001 remains OPEN; cutover BLOCKED; old-app replacement not yet verified.**
- **Never collect, request, paste, or store a real connector credential** (Okta/Google/Entra/Slack/SCIM/scraper/API token or key, service-account JSON) **until the credential vault is implemented + tested + reviewed** per [19_CONNECTOR_CREDENTIAL_VAULT_DESIGN](./19_CONNECTOR_CREDENTIAL_VAULT_DESIGN.md) (early note) — **the ACCEPTED design baseline + gated implementation sequence is now [42_CONNECTOR_CREDENTIAL_VAULT_DESIGN](./42_CONNECTOR_CREDENTIAL_VAULT_DESIGN.md) §19–§23 (PR #99/#100); start any connector/vault work there, NOT doc 19**. The confirmation pass (doc 18) confirms connector **existence/status only — never tokens.** Secrets never go in a Postgres column / generated types / client / logs; secret access is a future isolated out-of-request job (never the request DAL, never the browser, never service-role on a request path). RISK-007 is **open** — design ≠ closure.
- **Never run against hosted Supabase.** Local throwaway Postgres only (`scripts/test-rls.sh`).
- **Never mutate hosted Supabase** (no `supabase db push --linked`, no hosted apply, no schema/data change) **unless following [20_STAGING_HOSTED_APPLY_AND_CUTOVER_DISCIPLINE](./20_STAGING_HOSTED_APPLY_AND_CUTOVER_DISCIPLINE.md) AND explicitly approved by the human** — staging-first, clean tree, expected-migrations-only, verify-after-apply, never on type-drift/duplicate/non-green-RLS. An agent never initiates a hosted apply or production deploy on its own. RISK-001 is **open** — discipline ≠ apply. **⚠️ Staging status (PR #52/#55, evidence in [25_STAGING_SCHEMA_AND_STORAGE_APPLY_EVIDENCE](./25_STAGING_SCHEMA_AND_STORAGE_APPLY_EVIDENCE.md) §0/§0.2/§0.3):** a **human** has, in **staging** (`ycdpzduxugdsffjqyoai`) only, applied migrations `0001`–`0014`, created the private `contract-files` bucket (`public=false`, 25 MiB, `application/pdf`), **applied the `storage.objects` object policies** (2 `authenticated` INSERT/SELECT; **0 unsafe**), and **run the Storage REST verifier green — the real Storage REST API authorization verification PASSED in hosted staging (14/14, 2026-06-18; real REST calls with user-scoped JWTs, no service-role, no production touched).** **⚠️ Production status (PR #58, evidence [29_PRODUCTION_STORAGE_APPLY_EVIDENCE](./29_PRODUCTION_STORAGE_APPLY_EVIDENCE.md)):** a **human** has since executed the production apply on `dzbfxulvxchdemcettrx` — migrations `0001`–`0014`, the private `contract-files` bucket, 2 `authenticated` policies (0 unsafe) — and **the production Storage REST authorization verification PASSED 14/14** (real REST calls, user-scoped JWTs, no service-role, no secrets); a production-discovered `public.files` `authenticated` SELECT/INSERT grant is codified as migration `0015`. **Production synthetic cleanup is complete (PR #59, [29 §6](./29_PRODUCTION_STORAGE_APPLY_EVIDENCE.md)): synthetic Storage objects (safe path), Auth users, and business rows removed (counts 0); only 2 tenant rows + 3 append-only `audit_logs` rows retained as anchors (`reject_audit_mutation()` blocks audit DELETE).** **The CLI is re-linked back to staging (`ycdpzduxugdsffjqyoai`); the agent ran nothing against production.** Before ANY `--linked` command, **`cat supabase/.temp/project-ref` and confirm the intended ref** — an unintended `--linked` push is a hosted incident. **Both staging AND production Storage authz are now verified; but upload is NOT automatically production-ready** and no upload route/action/UI/signed-URL/AI has shipped. **An agent still never runs hosted mutations** (or the verifiers). **RISK-001 stays OPEN** — closure criteria (1)–(4) are met, but **(5) the [17 §5](./17_OMC_PRODUCTION_REPLACEMENT_PARITY_GATE.md) cutover checklist is NOT met**; **cutover BLOCKED** until it passes (and the doc 27 parity matrix is mostly missing).
- **Never use service-role keys** outside trusted server/test paths; never in the client.
- **Never weaken RLS**; never filter for security in the client.
- **Never edit a merged migration** (`0001`–`0013`) — fix forward with `000N_*.sql`.
- **Never re-add hard-delete** to core evidence tables (`organizations`/`apps`/`contracts`/`app_contracts`/`people`/`app_users`): no `FOR ALL`/`FOR DELETE` policy — write surfaces add `INSERT`+`UPDATE` only (`0004`, [02 §4b](./02_SECURITY_AND_RLS.md)). Archive/soft-delete UI is deferred (not built).
- **New tenant-scoped child/link table** ⇒ add a composite same-tenant FK `(parent_ref, tenant_id) → parent(id, tenant_id)` (and `UNIQUE (id, tenant_id)` on the parent) so cross-tenant references fail at the DB, not just hide under RLS (`0005`, [02 §5b](./02_SECURITY_AND_RLS.md)). Migrations are now `0001`–`0013`.
- **Never build UI ahead of its build-sequence prerequisites** ([06](./06_BUILD_SEQUENCE.md)).
- **Never expand telemetry** — no custom events, no PII/tenant/customer/business data in analytics, no new instrumentation, until a production privacy review ([04 · RISK-013](./04_RISK_REGISTER.md)).
- **Never hosted-apply the local fixture.** `supabase/fixtures/local_demo.sql` is local-only synthetic data; run it only via `bash scripts/seed-local-demo.sh` (throwaway container). Never add it to `supabase/migrations/`, never `supabase db push`, never point it at the linked project ([04 · RISK-015](./04_RISK_REGISTER.md)).
- **Data access goes through `src/lib/data/` (server-only, read-only, RLS-scoped).** Don't scatter raw Supabase queries in pages/components; don't import the DAL into a Client Component; don't pass a caller-supplied `tenant_id` as an authorization input (RLS decides). After any migration, regenerate types: `bash scripts/gen-types-local.sh` (local-only; never `--linked`).
- **Never claim something is verified** without command output.

## Always, every PR
1. Branch off `main`; do not commit to `main`.
2. Run the checks and paste real output:
   ```bash
   bash scripts/check-migration-safety.sh
   bash scripts/test-rls.sh
   bash scripts/check-auth-safety.sh   # if you touched src/
   bash scripts/check-docs-updated.sh
   bash scripts/pr-review-summary.sh
   ```
   For app/UI work also run `npm run lint`, `npm test`, `npx tsc --noEmit`, and `npm run build` —
   all four are now `ci-enforced` on every PR by `.github/workflows/app-ci.yml`. Keep the build
   deterministic: no `next/font/google` (use the system font stack in `globals.css`), no remote fetch at build.
3. Update docs per the [docs-update policy](./08_CODE_AND_DOCS_STANDARD.md#required-updates-per-change-docs-update-policy):
   at minimum add a [05_ENGINEERING_CHANGELOG](./05_ENGINEERING_CHANGELOG.md) entry; touch
   [04_RISK_REGISTER](./04_RISK_REGISTER.md) if risk changed.
4. Self-review against [07_P0_REVIEW_CHECKLIST](./07_P0_REVIEW_CHECKLIST.md).
5. Apply the [ponytail pass](./08_CODE_AND_DOCS_STANDARD.md#ponytail-pass-before-any-pr) — build the smallest safe thing.

## Connected agent permissions
**Canonical policy for connected coding agents and external tools** (Claude, Vercel, GitHub,
Supabase agents, and any future automation). Other docs link here; do not restate it.

These tools act under the same rules as a human contributor, with a hard ceiling: **they
propose on branches; humans dispose on `main`.** Nothing an agent does reaches `main`,
production, hosted Supabase, secrets, or DNS without human review.

**Allowed**
- Create branches.
- Edit files on branches.
- Open PRs.
- Run local checks (the scripts above; lint/build).
- Read CI / deployment status.
- Vercel may create **preview** deployments.

**Not allowed**
- Push directly to `main`.
- Auto-merge PRs.
- Bypass or disable CI.
- Modify repo secrets / add new secrets.
- Add or use service-role keys (see [non-negotiable rules](#non-negotiable-rules)).
- Run **hosted** Supabase migrations (local-only; hosted apply is a separate reviewed runbook PR).
- Change DNS / custom domains.
- Promote / approve **production** deployments.
- Silently add telemetry, analytics, auth, billing, imports, exports, or integrations **without** docs / risk / changelog updates.

**Required for every agent-generated PR**
- The [PR template](../.github/pull_request_template.md) is completed.
- Docs / risk / changelog updated, or a valid [`.docs-not-needed.md`](../.docs-not-needed.template.md) justification.
- CI is green.
- A human reviews before merge.
- No hosted Supabase changes unless a **deployment-runbook PR** explicitly authorizes them.

Rationale and the automation risk: [04 · RISK-014](./04_RISK_REGISTER.md). Reviewer enforcement: [07 · Connected agent PRs](./07_P0_REVIEW_CHECKLIST.md#connected-agent-permissions). Discipline for vendor/bot PRs: [08](./08_CODE_AND_DOCS_STANDARD.md#vendor-and-bot-agent-prs).

## Current next recommended task
**Legacy UX/workflow parity map is DONE — PR #28** ([14_LEGACY_UX_WORKFLOW_PARITY_MAP](./14_LEGACY_UX_WORKFLOW_PARITY_MAP.md)).
It is now the **contract that gates cutover**: every parity-bound PR must preserve the legacy user workflow (or get the difference
approved + documented there), and **cutover is blocked on workflow parity, not backend/RLS readiness**. Before claiming **Same** for any
workflow, inspect the running legacy app for exact fields/labels/filters/exports (doc 14 §9 — `needs legacy inspection`; do not invent).

**Contract audit-on-write is DONE — PR #29** (`0010`; DB-side `SECURITY DEFINER` `AFTER INSERT/UPDATE` trigger; T31/T32).
**Contract write PATH is DONE — PR #30** (server-side DAL + `"use server"` actions, anon client, RLS-gated, `tenant_id` server-resolved, audit inherited).
**Contract create/edit UI is DONE — PR #31** (`/contracts/new` + `/contracts/[id]/edit` posting to the #30 actions; RLS is the boundary, not client role checks).
**Contract form parity fields are DONE — PR #32** (`0011` adds `category`/`procurement_date`/`notes`/`po_number`/`auto_renew`/`month_to_month`; +7 unit tests; RLS unchanged by that PR). **Partial** legacy parity still ([15](./15_LEGACY_CONTRACT_FORM_INSPECTION.md)).
**Contract PDF/AI extraction is DESIGNED — PR #33** (security/design plan [16](./16_CONTRACT_PDF_AI_EXTRACTION_DESIGN.md); **nothing built** — no upload, Storage, AI call, `files` surface, or migration).
**The recommended next step is to IMPLEMENT the PDF/AI design (multiple PRs, each with tests), then the remaining parity gaps** —
every parity-bound step must inspect the running legacy app first (doc 14 §9 — `needs legacy inspection`; do not invent):
1. ~~**Contract audit-on-write**~~ — **DONE (PR #29, `0010`).** Invisible backend; no user-visible workflow changed.
2. ~~**Contract write path**~~ — **DONE (PR #30).** Server actions + DAL on the anon client; RLS is the boundary; `paying_org_id` never grants write; no `DELETE`/`FOR ALL`; no service-role; audit inherited.
3. ~~**Contract create/edit UI**~~ — **DONE (PR #31).** First user-visible write workflow; RLS-gated; generic denial (no enumeration); no delete/archive button.
4. ~~**Contract form parity fields**~~ — **DONE (PR #32, `0011`).** Added the schema-backed legacy fields. `commodity_*`/`validated` deliberately not added (hidden / read-only in legacy — docs/15).
5. ~~**Contract PDF/AI extraction DESIGN**~~ — **DONE (PR #33, [16](./16_CONTRACT_PDF_AI_EXTRACTION_DESIGN.md)).** Design only — legacy anti-patterns documented (service-role Storage fn, no prompt-injection defense, wholesale AI fields, auto-overwrite); v3 plan = assistive upload → server-validated, tenant-bound, signed-URL Storage → AI **suggestions only**, strict-allowlist parsed by `parseContractWriteInput` → user reviews+applies → save via the PR #30 RLS-gated action.
6. ~~**`files` metadata foundation**~~ — **DONE (PR #34, `0012`).** Additive `files` columns (`contract_id` same-tenant FK + `storage_bucket`/`content_type`/`byte_size`/`sha256`/`upload_status`/`scan_status`/`extraction_status`/`extraction_result_json`/`extraction_error`/`updated_at`) + CHECKs + indexes. **No RLS policy, no Storage/upload/AI/UI** — `files` stays default-deny / not surfaced. RLS suite 177 → **186** (T33). `gen-types` updated.
7. ~~**`files` RLS policies + §5 tests**~~ — **DONE (PR #35, `0013`).** SELECT `is_tenant_member` (tenant-member read; org-scoped deferred) + INSERT contract-write authority (`can_write_contract`: tenant editor+ OR procurement-org manager; **`paying_org` no write**; `uploaded_by`=caller); **no UPDATE/DELETE/FOR ALL**. T34 (+ T27/T33 updated); RLS suite 186 → **205**. `files` authorized-by-design + tested, still **not surfaced**.
8. ~~**OMC production replacement parity gate**~~ — **DONE (PR #36, docs-only — [17](./17_OMC_PRODUCTION_REPLACEMENT_PARITY_GATE.md)).** Created the binding go/no-go gate (grounded ~105-row replacement matrix, hard-blocker list, go/no-go checklist, OMC-confirmation list, honest ~70–110-PR estimate). **OMC = paying production replacement, NOT a pilot.** No migration/RLS/app/route change; RLS stays 205.
9. ~~**OMC confirmation pass scaffolding**~~ — **DONE (PR #37, docs-only — [18](./18_OMC_CONFIRMATION_PASS.md)).** The working questionnaire + workshop agenda + workflow confirmation table + decision log that resolves doc 17's `probably`/`unknown` rows from OMC evidence. **Feeds doc 17; does NOT make v3 ready; does NOT remove blockers by itself.** Unknown = blocker until confirmed (owner + date + evidence). **No secrets/tokens collected.** RLS stays 205.
10. **The canonical next-PR sequence is [17 §7](./17_OMC_PRODUCTION_REPLACEMENT_PARITY_GATE.md)** — two interleaved tracks: **A** = security/file path (server-side PDF **validation + storage-path helpers DONE PR #40** — `src/lib/files/pdf-validation.ts`; → the hosted private `contract-files` bucket + Storage object policies + the upload action [**user-scoped client + the `0013` insert authority; NO service-role app route**] → signed-URL read → extraction worker [out-of-request, tenant-re-deriving] + strict-allowlist parsing → review-and-apply UI → file/extraction audit → org-scoped `files` read), **B** = replacement-parity build-out (OPS hosted-apply gate, admin/auth, apps, connectors+credential vault, licenses/spend, people/identity, reports, billing). **First run the OMC-confirmation pass via [18_OMC_CONFIRMATION_PASS](./18_OMC_CONFIRMATION_PASS.md) — it sizes everything; record confirmations there, then update doc 17.** The Storage bucket + object-RLS are hosted-gated ([20](./20_STAGING_HOSTED_APPLY_AND_CUTOVER_DISCIPLINE.md)) — **PR #41 ([21](./21_STORAGE_LOCAL_HARNESS_FEASIBILITY.md)) empirically proved the local harness has no `storage` schema and object-RLS can't be faithfully tested locally; don't add a `storage` shim or a storage-policy migration (breaks `test-rls.sh`/`gen-types`). Verify the bucket/policies in hosted staging via the [21 §6](./21_STORAGE_LOCAL_HARNESS_FEASIBILITY.md) checklist before any upload action ships — the exact reviewed, human-executed staging apply runbook is [22_HOSTED_STORAGE_BUCKET_APPLY_RUNBOOK](./22_HOSTED_STORAGE_BUCKET_APPLY_RUNBOOK.md) (staging-only, stop before prod, nothing auto-applied), and the evidence template to fill in during that execution is [23_STORAGE_STAGING_APPLY_EVIDENCE_TEMPLATE](./23_STORAGE_STAGING_APPLY_EVIDENCE_TEMPLATE.md); the staging env-var inventory + Vercel/Supabase wiring checklist (names only, no values) is [24_STAGING_ENVIRONMENT_VARIABLES_AND_WIRING_CHECKLIST](./24_STAGING_ENVIRONMENT_VARIABLES_AND_WIRING_CHECKLIST.md). An agent NEVER executes a hosted apply / `supabase db push --linked` / bucket-or-policy creation / Vercel-or-Supabase env config itself — those are human-run under explicit approval (doc 20); an agent may only prepare the runbook/evidence/inventory docs (never with real secrets/values).** **Suggestions only — no AI auto-save; PDF text + AI output are untrusted.**
Alternative tracks (lower priority): the first reviewed **hosted-Supabase apply** (RISK-001); or an identity surface —
- **Richer "managed vs orphaned" status** (needs a tenant-only column): build it via a **`security_invoker` view** (caller RLS scopes it) — or a `SECURITY DEFINER` fn that re-derives scope — returning only a status enum; **NEVER** read `people`/`identity_accounts` rows into an org surface. Follow doc 12 §4 (the definer trap) + §7.7 (exact readable-only count test).

Do NOT org-scope `people` or `identity_accounts` — no app anchor; org-scoping them leaks the tenant-wide HR/IdP directory (doc 12 §4/§6). `people` stays **tenant-only**; `identity_accounts` stays **default-deny**.
Any account-intelligence work derives ONLY from visible `app_users` + visible matches (PR #24 pattern) — never read `people`/`identity` into an org surface, and do NOT relabel "unmatched/stale candidate" as "orphaned/deactivated/managed/UAR".
Still NOT safe to surface: `people`, `identity_accounts`, `invoices`/`files`/`license_*`. No identity matching algorithm / merge / UAR / orphaned status / provisioning exists yet.
(Stages 4/4b apps — PR #13/#14; Stage 5 contracts — PR #19; Stage 5b linked panels — PR #20; Stage 6a app-user roster — PR #21; Stage 6b identity read-scope design — PR #22; Stage 6c match status — PR #23; Stage 6d account summary — PR #24; Stage 5b contract write design — PR #25; truth pass — PR #26; `0009` app_contracts tenant-bind — PR #27; legacy UX/workflow parity map — PR #28; contract audit-on-write `0010` — PR #29; contract write path/DAL + server actions — PR #30; contract create/edit UI — PR #31; contract form parity fields `0011` — PR #32; contract PDF/AI extraction design — PR #33; files metadata foundation `0012` — PR #34; files RLS policies `0013` — PR #35; OMC production replacement parity gate doc 17 — PR #36; OMC confirmation pass scaffolding doc 18 — PR #37; connector credential vault design doc 19 — PR #38; staging + hosted apply & cutover discipline doc 20 — PR #39; contract-file PDF validation foundation `src/lib/files/pdf-validation.ts` — PR #40.)

## Current open risks to respect
**hosted apply PARTIAL** (`0001`–`0013` `staged` to staging `ycdpzduxugdsffjqyoai`; Storage object policies + verification + production still pending — RISK-001 OPEN, [04](./04_RISK_REGISTER.md)); child tables **not org-scoped for reads** — tenant-only (`people`) or default-deny (`identity_accounts`/`license_*`/`files`/`invoices`); `app_contracts` (`0006`) + `app_users` (`0007`) + `app_user_identity_matches` (`0008`) are now org-scoped read; see read map [02 §8](./02_SECURITY_AND_RLS.md) (RISK-002, narrowed not closed); no tenant switching /
user provisioning yet (RISK-012); no credential vault; imports/exports destructive-in-legacy
(don't port — legacy deletes "outdated" users, `onFileLinkedToApp.js:290`); v3 must not miss legacy
paid-client (OMC/Flywheel) capabilities (RISK-016). Full list:
[04_RISK_REGISTER](./04_RISK_REGISTER.md).

## Legacy parity (paid client)
When you build a product surface, update its row in [11_LEGACY_PARITY_AND_OMC_CHECKLIST](./11_LEGACY_PARITY_AND_OMC_CHECKLIST.md)
(status + OMC checklist) and link the PR. Verify legacy behavior from the legacy repo
(`/Users/samvemuri/Desktop/IDCaddie_Repo-main`) and [current-product-map.md](./current-product-map.md) —
never from memory; mark unverified claims `needs-verification`. Do **not** imply OMC can cut over until P0/P1 parity is verified.

## How to summarize at the end of a PR
State: what changed, what you **verified** (with the command output), what is still
unverified/deferred, which docs/risk/changelog you updated, and the next safe step. Do not
overstate. Surface uncertainty explicitly.

## How to avoid hidden context
If a fact matters, put it in a doc, test, script, CI check, the risk register, or the
changelog — **not** in chat or memory. The next agent should need nothing but the repo.
