# Public Supabase config — both are PUBLIC (safe in the browser). RLS is the auth boundary.
# Copy to .env.local and fill in. Never add SUPABASE_SERVICE_ROLE_KEY here or anywhere in src/.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
